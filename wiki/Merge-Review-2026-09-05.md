# 双源项目合并审查：2026-09-05

## 范围与原则

本轮以只读方式审查 `D:\Java\GitHub\xuanshu-z` 和 `D:\Java\GitHub\xuanshu`，再把可验证的能力按当前仓库的模块边界重写到 `xuanshu-plus`。没有覆盖源目录，也没有用整目录复制替换当前实现。

合并遵循四条准则：现有能力不降级、领域算法不进入 React、规划能力不冒充已实现能力、每组独立改动先本地验证再单独提交和推送。

## 合并矩阵

| 来源 | 候选能力 | 处理 | 当前落点与理由 |
|---|---|---|---|
| `xuanshu-z` | 万年历、黄历宜忌、节气与干支日信息 | 重写合入 | `packages/core/src/calendar/almanac.ts` 与懒加载 `AlmanacView`；保留 1900-2100 日期校验、月视图和日详情 |
| `xuanshu-z` | 月历界面 | 重写合入 | 七列稳定网格，手机 360px 无整页横向滚动，按钮满足 44px 触控高度 |
| `xuanshu-z` | 八字趋势 | 修复后复用当前实现 | 审查发现十神评分把大运天干与自身比较，导致恒为“比肩”；已改为与日主比较并增加回归测试 |
| `xuanshu-z` | 16 部、约 1516 段语料 | 不替换 | 当前仓库已有 18 部、14200 段且具备稳定引用回链；替换会造成内容和引用覆盖率倒退 |
| `xuanshu-z` | 合婚、称骨、流年建议等扩展模块 | 暂不直接复制 | 尚未接入当前插件契约、证据引用和黄金样本；需要逐功能校勘后再独立合入，不能以可编译代替正确性验证 |
| `xuanshu` | 六爻与奇门的日期化应期窗口 | 重写合入 | `packages/core/src/timing.ts`；只对规则足以推导准确日期的术数输出时间线，其他术数明确返回无时间线 |
| `xuanshu` | 应期回标与统计 | 重写合入 | `packages/ledger/src/timing.ts`；区分待观察、到期、提前应验和日期偏差，不把未判断记录算作未应验 |
| `xuanshu` | Web/桌面/移动平台适配器 | 按现状重写 | `packages/core/src/platform/index.ts`；桌面允许 `safeStorage`，Web/Android 当前只允许内存 Key |
| `xuanshu` | Android 加密存储、原生通知和分享 | 不标记为已完成 | 当前 `xuanshu-plus` 未安装对应 Capacitor 插件；能力矩阵保留为受限或不可用，避免误导和不安全降级 |
| `xuanshu` | 模块化 UI 草案 | 选择性吸收 | 万年历、时间线和回标已拆为独立组件；没有覆盖当前 `App.tsx` 中更完整的八术、知识库和安全流程 |

## 同步修复

| 问题 | 修复 |
|---|---|
| 正时区清晨可能因先转 UTC 再截日而误落前一日 | 九个排盘/归一化入口统一用输入的本地公历日期计算 `civilJdn`，绝对 `jd` 仍用于天文计算 |
| Web/Android 点击“安全保存 Key”后提示仅内存却清空唯一副本 | 非桌面端改为“仅在本页使用”，保留 React 内存值；仅桌面 `safeStorage` 写入成功后清空输入 |
| Android 文案被笼统称为 Web 版 | 通过能力适配器识别 Capacitor/Android UA，并显示当前运行壳的真实存储和联网边界 |

## 分批提交

| 提交 | 内容 |
|---|---|
| `d6bef59` | 万年历核心与八字趋势评分修复 |
| `6458fdd` | 日期化时间线和应期回标存储 |
| `d2c5503` | 万年历、时间线、回标的响应式 Web 集成 |
| `c4416ef` | 跨引擎本地民用日期一致性修复 |
| `27f9c95` | Web、Electron、Android 能力矩阵 |
| `21ae25c` | 按平台执行 AI Key 安全边界并修复误清空 |

## 验证证据

- 单元测试：24 个文件、295 项全部通过。
- 类型检查：7 个领域包、Web 和 Electron 全部通过。
- Web 生产构建：通过；万年历与农历依赖保持懒加载。
- 浏览器响应式验收：360×800、390×844、768×1024 无整页横向溢出；手机按钮最低 44px。
- 功能验收：月历换月与日详情、六爻日期时间线、存档生成回标、结果回标、非桌面 Key 保留均已实际操作验证。

算法测试证明实现满足当前契约和黄金样本，不等同于完成独立学术校勘。新增传统算法仍应附来源说明、边界条件和交叉样本。
