# 玄枢 xuanshu · 五术综合占卜工作台 开发计划（完整合并版）

> **版本：v5（合并完整版）**　**日期：2026-08-29**（Asia/Shanghai）
> **基线**：v1 `dawn-ravine-finch.md` → v2 → v3 → v4 → **本版合并 v3 + v4，补齐交叉缺漏**
>
> **七大核心决策（v3 五项 + v4 两项，全部锁定）**
> ① Android 16/17 双轨适配　② Windows 11（Electron）适配　③ 每术 99 条占卜记录 + 主动标注　④ 按事项精准答复与用户引导　⑤ **按事项的「指引排盘」——失物/姻缘/事业/过去未来等每类一套九段断事路径**　⑥ **典籍查阅器 + 批注溯源双向跳转（引用五档分级）**　⑦ **历法内核可复算、可测试（v3 §4，本版补全）**
>
> **原则**：v3 / v4 已锁定的决策全部继承；本版只做合并与缺漏补齐，**不推翻、不重复论证**。

---

## 0. 变更摘要（合并 v3 + v4，按七大补充组织）

### 0.1 补充一：Android 16 / 17 双轨适配（v3 锁定）

**「能在新系统上运行」≠「面向新系统适配」：** 前者由 `minSdkVersion` 决定，后者由 `targetSdkVersion` 决定，而后者是 Play 合规硬指标。

**Play 合规硬指标（检索日 2026-08-29，来源已标注）**：自 **2026-08-31** 起，提交 Play 的新应用与更新须 target **Android 16（API 36）** 或更高（可申请延期至 **2026-11-01**）；现有应用需 target 35+ 才能对更高系统新用户可见；**API 37 暂无强制死线**。

**Capacitor ↔ target SDK 强绑定**（官方不支持自定义 target）：9.x→37、8.x→36、7.x→35。故采用**双轨**：

| 轨道 | 版本组合 | 角色 |
|---|---|---|
| **主线（发布基线）** | Capacitor 8 + compile/targetSdk **36** + AGP 8.13 + Gradle 8.14.3 + JDK 21（minSdk 24） | 满足 Play 死线，P5 出包 |
| **前瞻轨** `android-17-preview` | Capacitor 9 + compile/targetSdk **37** + AGP 9.2 + Gradle 9 + **minSdk 26** | 验证 Android 17 行为变更，真机通过后合并主线 |

**「支持 Android 17」兑现方式**：target 36 应用可在 Android 17 设备安装运行；前瞻轨再做 target 37 适配。**Android 17 的 11 条行为变更中，仅三条真影响本项目**：① 大屏强制自适应（sw>600dp 忽略固定方向）② Activity 不再因配置变更重建（状态须自恢复）③ 若做局域网功能才需 `ACCESS_LOCAL_NETWORK`。

**切换主线条件**（全部满足）：Capacitor 9 GA、Android 17 真机全场景通过、大屏/折叠屏验收、国产 ROM WebView 无致命差异、第三方插件均支持 Capacitor 9。

### 0.2 补充二：Windows 11（Electron）适配（v3 锁定）

- **版本**：Electron 43.x–44.x（官方支持 Windows 10 及以上，提供 x64/arm64/ia32）。
- **体验项**：`backgroundMaterial:'mica'`、保留原生标题栏以获 Snap Layouts、系统主题/强调色跟随（`nativeTheme.themeSource='system'`）、原生通知、JumpList、任务栏进度、`.xuan-case.json` 文件关联。
- **安全**：`contextIsolation:true`、`sandbox:true`、CSP；AI Key 存 `safeStorage`（Windows=DPAPI），不写配置、不进日志。
- **分发**：NSIS x64（主）+ portable + 可选 arm64/MSIX；OV/EV 代码签名，未签名须 UI 明示风险并提供 SHA-256。

### 0.3 补充三：每术 99 条占卜记录 + 主动标注（v3 锁定，核心增量）

新增 **`ledger`（记录本）包**：

- **配额**：八种术数（八字/六爻/梅花/奇门/大六壬/小六壬/金口诀/紫微）**各 99 条独立配额**，互不占用；90 条软提醒、99 条引导归档/导出；**不静默删除**，超限进本地归档区可恢复；上限可配（99/199/无限+阈值）。
- **记录内容**：完整可复现快照——原始输入 + 归一化时刻 + 完整配置（`configHash`）+ 引擎版本 + 盘面 JSON + 规则命中 + **`CitationRef[]` 引用原文** + AI 判断卡（provider/model/promptHash）+ 用户标注。**任意一条可一键还原当时盘面。**
- **主动标注**：预设标签（应验/部分应验/未应验/存疑/重要）+ 自定义、结果反馈（含时间）、评分、备注、`keyTakeaway`（这次学到什么）、`matchedRuleIds`、关联同事项多次占、回看提醒。
- **本地闭环统计**：按术数/事项/规则/用神维度比对「当初怎么断」与「实际结果」。**只校准用户自己的解释习惯，绝不回写排盘层**——排盘层是不可变事实源；样本<20 条时显式标注「样本不足」。
- **导出**：`.xuan-case.json` / CSV / Markdown（含盘面、证据、标注、出处）。

### 0.4 补充四：按事项精准答复 + 用户引导（v3 锁定）

新增 **`intake`（事项引导）+ `answer`（答复装配）** 两包：

- **事项分类体系 `QuestionTaxonomy`**（14 类：财运/事业/感情/学业/健康/出行/官非/失物/择日/家宅/生育/合作/决策/其他），每类定义：推荐术数、配置预设、关键因子（应期/方位/数字/人物/颜色）、引导话术、答复骨架、禁用项。
- **6 步引导式起卦向导 `IntakeWizard`**：选事项 → 细化问法 → 推荐术数与配置 → 输入/起卦 → 出盘逐格解释 → 记录标注；全程即时提示「为什么这么问」「怎么问才有效」。
- **精准答复 = 四层装配（不靠模型自由发挥）**：`AnswerComposer` = 事项模板 + 计算层事实（用神旺衰/空破/刑冲/应期候选）+ 知识层原文（BM25 命中带出处）+ AI 层（仅语言组织与权衡，受 schema 约束、无证据即报缺口）。
- **应期/取象规则化**：六爻/梅花/奇门/六壬/八字的应期推法做成带 `ruleId` 的计算规则；AI 只排序与语言化，且必须引用 `ruleId`。

### 0.5 补充五：按事项的「指引排盘」（★ v4 新增）

v3 的 `intake` 只做到「选事项 → 推荐术数 → 配置预设」。v4 升级为 **`playbook`（断事路径卡）**：每个事项给**九段完整可执行路径**——术数选择 / 怎么问 / 怎么起 / 取用神 / 看什么 / 定方位取象 / 断应期 / **读哪本书（★ 与典籍查阅器打通）** / 怎么记。

**关键约束**：`yongShen / signals / locating / timing` 每条必须带 `ruleId` + `citations(CitationRef)` + `confidenceLevel`。**无原典出处的民间口诀一律标 D 级「流派说法」，与 A/B 级原典分区展示，绝不混排。** 完整示例见 §7（失物/姻缘/事业/过去未来）。

### 0.6 补充六：典籍查阅器 + 批注溯源双向跳转（★ v4 新增）

新增 **`reader` 包**：知识库从「检索数据源」升级为「可阅读、可批注、可跳转的典籍库」。

- **引用模型 `CitationRef`**：`canonicalId + segId + charRange` 精确定位到「哪本书、哪章、哪段、哪几个字」，并带五档可信度。
- **三层定位**：**L1（主）自研 `charRange` 段内定位**（三壳一致、离线可靠） / **L2 Text Fragments** `#:~:text=`（仅对外分享链接）/ **L3 IIIF Canvas `#xywh=`**（P6 影印本）。
- **双向跳转**：正向（断语→原文，点角标高亮） + 反向（原文→断语，点「被引用」反查卦例/规则）。
- **批注闭环**：用户在书上划词批注 → 下次排盘命中该段时，批注随原文一起出现在断语里。**读过的书变成自己的解释能力。**
- **版本对照**：多版本并列 + 差异标注，**不自动判定优劣**（自动校勘需专家参与）。
- **引用五档分级**：A 原典 / B 注疏 / C 现代整理 / D 流派说法 / E AI 生成；D/E 强制与 A/B 分区展示。

### 0.7 补充七：历法内核可复算、可测试（v3 §4，本版从 v3 补全）

自研内核目标**可控、可测试、可复算，不宣称比成熟天文库更精确**：JDE 中心、太阳视黄经（Meeus）、均时差、真太阳时（经度差+时区，**北京时间≠真太阳时**）、节气逆迭代求交 JDE；农历/闰月**不自研**，委托 `lunar-javascript`（锁版本 + `LunarAdapter`）；sxtwl 仅比对日期/节气/干支。

**精度表述（写进 UI，不夸大）**：1900–2100 默认设置下与公开历表交叉验证；高精度节气仅研究模式；超出已验证年份显式「超出支持范围」；未做全量天文年历逐秒比对前**不宣称毫秒级**。

### 0.8 合并版决策台账（v3 D1–D9 + v4 D10–D17，共 17 项）

| # | 维度 | 决策 |
|---|---|---|
| D1 | Android 发布基线 | Capacitor 8 + targetSdk 36（满足 2026-08-31 Play 强制） |
| D2 | Android 17 | 独立分支走 Capacitor 9 + target 37，真机验证后合并 |
| D3 | minSdk | 主线 24 → 前瞻轨 26（Capacitor 9 要求） |
| D4 | 桌面目标 | Win11 为主，Win10 21H2+ 尽力；Electron 43/44 |
| D5 | 记录配额 | 每术 99 条 + 软提醒 + 归档（不静默删）+ 可配上限 |
| D6 | 标注闭环 | 本地统计，不回写排盘层，不云同步 |
| D7 | 精准答复 | 模板 + 事实 + 原文 + 受约束 AI 四层装配 |
| D8 | 事项引导 | 结构化 intake + 问句质量提示 + 澄清追问 |
| D9 | iOS | 仅存根，Windows 不可构建，不承诺 |
| **D10** | **跳转主机制** | **自研 `charRange` 段内定位（Text Fragments 在 WebView/iframe/中文古籍下不可靠）** |
| **D11** | **影印本定位** | **预留 IIIF Canvas `#xywh=`（P6）** |
| **D12** | **批注来源** | **五档可信度分级，UI 明示；民间整理≠古籍原解** |
| **D13** | **版本对照** | **多版本并列 + 差异标注，不自动判定** |
| **D14** | **引导形态** | **每事项一张 `playbook`（九段路径卡）** |
| **D15** | **playbook↔reader** | **通过 `readingList` 打通，点读直达原文章节** |
| **D16** | **playbook 规则来源** | **每条 `ruleId` 必带原典引用；民间口诀只能标 D 级** |
| **D17** | **过去未来** | **拆分「复盘/验卦/来意」与「趋势/应期」，明示能力边界** |
| **D18** | **紫微斗数** | **安星委托 `iztro`（锁版本 + `ZiweiAdapter`），不自研安星；流派配置进 `configHash`** |

---

## 1. 产品定位与差异化（合并五层架构）

**一句话**：玄枢不是「调大模型生成算命文案」的聊天框，而是**排盘确定性 + 解释开放性 + 路径可学习**的传统历法与文化研究工具。

| 层 | 职责 | 关键约束 |
|---|---|---|
| **排盘层** | 离线、纯函数、可复算 | 绝不交给模型推四柱/卦象/世应/三传 |
| **引导层** | 把模糊的「我想问问」变成结构化问事 | 问句质量提示 + 澄清追问 |
| **★ 指引层** | 每事项一套断事路径：怎么问、怎么起、取什么用神、看什么信号、读哪本书 | 每条规则带 `ruleId` + 原典引用 |
| **知识/阅读层** | 原典可阅读、可批注、可跳转、可反查；引用五档分级 | 无证据即报缺口；民间说法不得冒充原典 |
| **AI 层**（默认关闭） | 只做语言组织与权衡 | 无证据即报缺口，禁止补充成「古籍观点」 |

**差异化（对竞品）**：高赞评价集中在「解析详细、清晰、可学习」。玄枢做到极致——**不止告诉你结论，还告诉你出自哪本书哪一章，点一下就能跳过去读原文；读完在原文做笔记，笔记会在下次排盘时回来找你。**

| 竞品能力 | 玄枢做法 | 差异点 |
|---|---|---|
| 多流派配置 | 每个流派一个 `config profile`，全进 `configHash` | 可复现、可 diff |
| 点击宫位/爻位出古籍原文 | `canonical_id` 回链，无命中显式报缺口 | 可追溯，不伪装 |
| 内置古籍阅读 | 公共版权原本内置 + 用户自有书库导入（`user-owned`，不云同步） | 版权分层 + **可批注可跳转** |
| 会员/元宝课程 | 先做可审计功能包，会员后置 | 不卖「准确率」 |
| — | **指引排盘（playbook）+ 99 条案例本 + 双向溯源** | 从「给结果」到「给可学习的过程」 |

**商业化节奏**：先功能包（离线排盘、内置古籍、无限本地记录、导入书库、JSON 导出、多端同步工程位），再考虑订阅。不销售「预测准确率」「医疗/投资建议」「改运效果」。

---

## 2. 平台适配专章（Android + Windows，v3 §2 完整保留）

### 2.1 Android 版本事实表

| 版本 | API | 状态 | 与本项目关系 |
|---|---|---|---|
| Android 16 | 36 | 2025-06 发布 | Play 自 2026-08-31 强制 target ≥36 |
| Android 17 | 37 | 2026-06-16 推送 Pixel，行为变更文档 2026-07-14 更新 | 前瞻轨 target 37；主线在 17 真机验收「运行」 |

### 2.2 Capacitor ↔ target SDK 矩阵

官方矩阵：9.x→37、8.x→36、7.x→35、6.x→34、5.x→33，**不支持自定义 target SDK**。想「支持安卓 17」等价于「升级到 Capacitor 9」，无法靠手改 `variables.gradle` 绕过。

### 2.3 双轨策略细则

**主线（Capacitor 8 / target 36）**

```
android/variables.gradle
  minSdkVersion        = 24
  compileSdkVersion    = 36
  targetSdkVersion     = 36
  androidxActivityVersion = '1.9.x'
  coreSplashScreenVersion = '1.0.1'
android/build.gradle   → AGP 8.13.x
gradle/wrapper         → Gradle 8.14.3
JDK                    → 21
```

**前瞻轨（Capacitor 9 / target 37）**

```
android/variables.gradle
  minSdkVersion        = 26
  compileSdkVersion    = 37
  targetSdkVersion     = 37
  androidxActivityVersion = '1.13.0'
  androidxAppCompatVersion = '1.7.1'
  androidxCoreVersion  = '1.19.0'
  androidxFragmentVersion = '1.8.9'
  coreSplashScreenVersion = '1.2.0'
  androidxWebkitVersion = '1.16.0'
android/build.gradle   → AGP 9.2.x；移除 defaultConfig 中 targetSdkVersion（AGP 9 从 compileSdk 推断）
                        → proguard 改为 proguard-android-optimize.txt
                        → 移除 core-ktx（合并进 androidx.core:core:1.19.0）
                        → 移除 kotlin-android 插件与独立 kotlin-gradle-plugin（AGP 9 内置 Kotlin 2.2.10）
                        → 移除 jcenter()
gradle/wrapper         → Gradle 9.x
JDK                    → 21
```

**切换条件**（全部满足才合并主线）：① Capacitor 9 已 GA（非 `next` 通道）② Android 17 真机跑通全部端到端场景 ③ 大屏/折叠屏验收通过 ④ 国产 ROM WebView 无致命差异 ⑤ 第三方插件均声明支持 Capacitor 9。**回退**：任一不满足则主线保持 Capacitor 8/target 36，仍满足 Play 合规。

### 2.4 targetSdk 36（Android 16）必须处理项

| 变更 | 影响 | 应对 |
|---|---|---|
| Edge-to-edge 强制（API 35+） | 盘面被状态栏/导航栏/挖孔遮挡 | WebView 用 `safe-area-inset` + `viewport-fit=cover`；盘面容器不写死高度 |
| 预测返回手势 | 手势返回可能直接退出 App | 接 `popstate` + Capacitor `App.backButton`；向导每步可回退；末屏二次确认退出 |
| 大屏（sw>600dp）忽略方向限制（target 36 起，17 彻底失效） | 折叠屏/平板盘面拉伸黑边 | 容器查询 + 响应式栅格；**禁止依赖固定横竖屏**；奇门九宫格、六壬天地盘须自适应 |
| 健康权限细化、Photo picker 强制 | 文件导入路径变化 | 走系统文件选择器（SAF/`ACTION_OPEN_DOCUMENT`），不申请存储全量权限 |
| OpenSSL 3.x 默认 TLS | 证书固定可能失败 | **不做证书固定**；AI 走系统 TLS |
| Safer intents | 分享/导出可能失败 | 显式 intent 或系统分享面板 |
| 通知权限（13+） | 回看提醒需权限 | 按需申请，拒绝降级为应用内角标 |

### 2.5 targetSdk 37（Android 17）行为变更与本项目的应对

Android 17 面向 target 37 有 11+ 条行为变更，**逐条过完后真正影响玄枢的仅三条**（详见附录 A 核对表）：① 大屏强制可调整大小（§2.4 同一套自适应）② `MessageQueue` 无锁 / `static final` 不可改 → 禁用反射私有 API 的库 ③ **新增 `ACCESS_LOCAL_NETWORK` 权限**（仅 P6 做局域网同步时需申请）④ **Activity 不再因配置变更重建** → 向导进度/盘面滚动/未保存输入须持久化到 Dexie，自行刷新 ⑤ CJKV IME 无障碍增强（正向，中文输入更好）。其余为低影响或正向（证书透明度、ECH 调试影响等）。

**结论**：Android 17 工程成本可控，值得做但放在独立分支。

### 2.6 16KB 页大小与原生库

Android 15 起支持 16KB page size，Play 对运行在 16KB 设备的 64 位应用有原生库对齐要求（2025-11-01 起）。策略：**尽量零自研原生库**，只用官方 Capacitor 插件；构建后 `readelf -l` 校验 `.so` 对齐；若引入含 `.so` 的插件，在前瞻轨做 16KB 冒烟。**以 Play 政策页当日内容为准，构建后实测。**

### 2.7 真机 / 模拟器测试矩阵

| 环境 | 用途 | 最低要求 |
|---|---|---|
| Android 16（API 36）真机 | 主线发布验收 | ≥1 台 |
| Android 17（API 37）真机/emulator | 前瞻轨 + 主线「能运行」验收 | ≥1 台，真机优先 |
| 折叠屏/平板（sw>600dp） | 大屏自适应 | ≥1 台（展开/折叠/分屏/桌面窗口） |
| Android 13/14 低配机 | minSdk 回归与性能下限 | ≥1 台 |
| 国产 ROM（HyperOS/ColorOS/OriginOS） | WebView 差异、后台、通知 | ≥1 台 |
| Windows 11 23H2/24H2/25H2 | 桌面主目标 | 各版本冒烟 |
| Windows 10 21H2+ | 尽力兼容 | ≥1 台（虚拟机可） |

**必测场景**：冷启动、盘面渲染、向导回退、预测返回、edge-to-edge、大屏折叠、深浅色、通知、导入导出、AI 联网、**飞行模式离线**、配置变更后状态恢复（§2.5-④）、**阅读器长章节滚动与高亮（v4 新增）**。

**性能门槛**：1000 盘渲染不卡；BM25 检索 P95 < 200ms（本地 1 万段）；知识库导入 50/200/1000 文档不阻塞主线程（Web Worker）；中低端安卓冷启动 ≤3s。

### 2.8 Windows 11 适配（Electron）

- **版本**：Electron 43.x 或 44.x（构建宿主 Node 22.17.0 满足官方要求）。官方支持 Windows 10 及以上、提供 x64/arm64/ia32、Windows on ARM 自 5.0.8 起支持。
- **窗口外观**：`backgroundMaterial:'mica'`、保留原生标题栏以获 Snap Layouts、右键菜单与系统菜单。
- **主题**：`nativeTheme.themeSource='system'` 跟随深色/浅色与强调色；五行色在深色模式重新取色保对比度。
- **系统集成**：原生通知、JumpList（最近案例）、任务栏进度、`.xuan-case.json` 文件关联、系统分享。
- **安全**：`contextIsolation:true`、`sandbox:true`、CSP；AI Key 存 `safeStorage`(DPAPI)，不写配置、不进日志。
- **数据**：`%APPDATA%/xuanshu`（配置、Dexie、知识库、案例本），备份导出为用户自选目录。
- **更新**：electron-builder + 自建源或 GitHub Releases；Windows 更新须签名，否则 SmartScreen 拦截。

### 2.9 构建与分发

产物：NSIS x64（主）+ portable zip + 可选 arm64（Windows on ARM）+ 可选 MSIX。签名：OV/EV 代码签名证书；无签名须 UI 明示风险、提供 SHA-256，不伪装已签名。目标系统：Win11（23H2/24H2/25H2）为主，Win10 21H2+ 尽力。

### 2.10 iOS 存根与不可构建声明

`apps/ios` 保留 Capacitor 工程存根；Windows 上**不可构建**（缺 macOS/Xcode）；GitHub Actions 仅 macOS runner 执行 `cap sync ios` 冒烟。Apple 审核指南 4.3 将 fortune telling 与手电筒、壁纸等并列——iOS 目标后置，先用 Web/Electron/Android 证明差异化，取得 macOS 资源与法律意见后再行动。

---

## 3. 总体架构

### 3.1 目录结构（合并 v3 + v4：`ledger` / `intake` / `answer` / `reader` 四包齐备）

```
D:\Java\GitHub\xuanshu\
├─ .npmrc / .env.node / package.json          npm workspaces；Node 绝对路径锁定
├─ packages/
│  ├─ core/                                   纯 TS，零 DOM/Node 依赖，vitest 全覆盖
│  │  ├─ astronomy/                           JDE、Meeus 太阳视黄经、均时差
│  │  ├─ calendar/{solarTerms,ganzhi,lunar,solarTime,calibrate}.ts
│  │  ├─ board/schema.ts                      BoardSpec：plate|grid|table|ring|stack|list
│  │  ├─ plugins/{contract,registry}.ts
│  │  └─ arts/{bazi,liuyao,meihua}/  (P2)   {qimen,liuren,xiaoliuren,jinkou}/ (P4)
│  ├─ ledger/          ★v3                    案例本：配额、标注、闭环统计、导入导出
│  │  ├─ schema.ts                            CaseRecord / Annotation / FeedbackStats
│  │  ├─ quota.ts                             99 条配额、软提醒、归档与恢复
│  │  ├─ store.ts                             Dexie 事务层，崩溃恢复
│  │  ├─ feedback.ts                          本地闭环统计（只校准解释，不改排盘）
│  │  └─ io.ts                                .xuan-case.json / CSV / Markdown
│  ├─ intake/          ★v3                    事项引导
│  │  ├─ taxonomy.ts                          事项分类词典（附录 B）
│  │  ├─ playbooks/          ★v4              断事路径卡（九段，每事项一份）
│  │  │   ├─ liuyao-lost.ts                  失物
│  │  │   ├─ liuyao-love.ts                  姻缘
│  │  │   ├─ liuyao-career.ts                事业
│  │  │   ├─ past-future.ts                  过去未来（三条路径拆分）
│  │  │   └─ …（出行/官非/择日/家宅/健康/学业/决策 等）
│  │  ├─ wizard.ts                           6 步向导（★v4 融入九段路径）
│  │  ├─ recommend.ts / quality.ts
│  ├─ answer/          ★v3                    答复装配
│  │  ├─ composer.ts                          模板 + 事实 + 原文 + AI 四层装配
│  │  ├─ templates/{bazi,liuyao,meihua,qimen,liuren}.ts
│  │  ├─ timing.ts                            应期推法（ruleId 化）
│  │  └─ safety.ts                            敏感项拦截与免责注入
│  ├─ reader/          ★v4                    典籍查阅器
│  │  ├─ citation.ts                          CitationRef 模型与校验
│  │  ├─ locate.ts                            L1 charRange / L2 Text Fragment / L3 IIIF 预留
│  │  ├─ catalog.ts                           书架与目录树（卷/章/节/段）
│  │  ├─ render.ts                            正文分层渲染（原文/注疏/白话）
│  │  ├─ annotate.ts                          用户批注（划词 + CitationRef）
│  │  ├─ backlink.ts                          反查：段 → 卦例/规则
│  │  ├─ collate.ts                           版本对照（并列 + 差异标注，不判定）
│  │  └─ progress.ts                          书签与阅读进度
│  ├─ knowledge/                              BM25、导入管线、引用图、缺口回填
│  │  └─ importer/          ★v4                导入时保留章节层级（volume/chapter/section/segId）
│  ├─ ai/                                     ProviderRegistry、prompt 装配、调用审计
│  ├─ storage/                                Dexie / safeStorage / EncryptedSharedPreferences
│  ├─ ui/                                     React：BoardRenderer / 向导 / 断语抽屉 / 记录本 / 阅读器
│  └─ desktop-services/                       Electron 主进程：导入、AI 代理、加解密、备份
├─ apps/{web,desktop,android,ios}             Vite / Electron / Capacitor / 存根
├─ fixtures/                                  黄金样本 + 期望 JSON + 公开站快照
├─ data/.kb/books/<canonicalId>/corpus.jsonl  语料（带章节层级 + 五档分级）
├─ data/{cases,kb-gaps.jsonl,inbox,archive,notes}
├─ scripts/{run,doctor,gen-golden,verify-board,verify-citation,verify-playbook}.mjs
└─ docs/{verification.md,compliance.md,citation-policy.md,adr/}
```

### 3.2 分层与依赖规则

```
ui ─┬─ intake ─┬─ core(arts)
    ├─ answer ─┤
    ├─ ledger ─┤
    ├─ reader ─┤
    └─ knowledge
                └─ ai（独立，默认关闭）
```

- `core` 不依赖任何上层；`ledger/intake/answer/reader` 只依赖 `core` 的类型与纯函数，不反向依赖 `ui`。
- `ai` 只接收不可变 JSON（盘面 + 配置 + 检索片段），**不得调用 `core.compute` 之外任何推算入口**。
- 平台能力（文件系统、密钥、通知、分享）一律经 `PlatformAdapter` 注入，`ui` 不直接碰原生。

### 3.3 PlatformAdapter 与三壳能力矩阵

| 能力 | Web | Electron（Win11） | Android（Capacitor） |
|---|---|---|---|
| 排盘 / 记录 / 检索 | ✅ 全量 | ✅ 全量 | ✅ 全量 |
| 知识库存储 | IndexedDB(Dexie) | IndexedDB(Dexie) | IndexedDB(Dexie) |
| 导入本地文件 | File API（浏览器沙箱） | ✅ 原生对话框 + 全盘 | ✅ SAF |
| AI Key 安全存储 | ⚠️ 仅内存（不落盘，提示风险） | `safeStorage`(DPAPI) | `EncryptedSharedPreferences` |
| AI 请求代理 | 直连（受 CORS 限制） | 主进程代理（无 CORS） | 原生层代理 |
| 通知提醒 | Notification API | 原生通知 | 原生通知 + 渠道 |
| 备份导出 | 下载文件 | 自选目录写文件 | 分享面板 |
| 抓取回填（kb:fill） | ❌ | ✅ | ❌（桌面专用） |
| **阅读器长文本** | ✅ | ✅ | ✅（**须虚拟滚动，低配机实测**） |

> ⚠️ Capacitor `Preferences` 只是轻量 KV，**绝不能存密钥**；只能存 `providerId/baseUrl/model/lastUsed`。Android 密钥必须走 `EncryptedSharedPreferences`（AndroidX Security，Keystore + AES）。

### 3.4 插件契约 v5（含 intake / answer / playbook / reader 声明）

```ts
export interface ShuPlugin<I = unknown, C = unknown> {
  id: string; name: string; version: string;
  category: 'paipan' | 'kb' | 'ai';
  configSchema: JSONSchema;                                   // 自动表单 + 运行时校验
  normalize(input: RawInput, ctx: EngineCtx): NormalizedMoment;
  compute(input: I, ctx: EngineCtx, cfg: ResolvedConfig): C;  // 纯函数：禁 Date.now()/IO
  rules(chart: C, cfg: ResolvedConfig): RuleHit[];            // 每条带 ruleId + citations
  board(chart: C, cfg: ResolvedConfig): BoardSpec;
  evidence(chart: C, rules: RuleHit[]): EvidenceRequest[];
  warnings(chart: C, cfg: ResolvedConfig): Warning[];
  knowledgePack: { id: string; refs: string[] };
  fixtures: Array<{ input: I; gold: string }>;

  // ★v3 事项引导 + 答复
  intake: {
    categories: CategoryId[];
    presetFor(category: CategoryId, sub: SubCategoryId): Partial<ResolvedConfig>;
    guidance(category: CategoryId): GuidanceBlock;
    keyFactors(category: CategoryId): KeyFactor[];
  };
  answer: {
    templateFor(category: CategoryId): AnswerTemplate;
    timingRules(chart: C, cfg: ResolvedConfig): TimingCandidate[];
    extractFacts(chart: C, category: CategoryId): FactBundle;
  };

  // ★v4 指引排盘（playbook）
  playbook?: Playbook;   // 九段断事路径卡（见 §6）；每术可关联多张（按事项）
}
```

所有输出 JSON 携带 `configHash`（节气/经度/子时/流派/事项相关设置全进 seed），保证任何刷新可还原、可 diff。**插件代码构建期打包，热加载只放数据**（知识包、盘式配置、参数默认值均为 JSON）。

---


## 4. 历法内核（可复算、可测试，v3 §4 完整保留）

**自研内核的目标是可控、可测试、可复算，不是宣称比成熟天文库更精确。**

- `astronomy`：以 JDE 为中心，太阳平黄经 → 中心差 → 章动/光行差 → 视黄经；对 0°/15°/…/345° 逆迭代求交节 JDE。
- `solar-longitude`：Meeus 简化公式，服务日常节气与真太阳时，参数版本化。
- `solar-longitude-vsop87`（研究模式，可选）：抽样复核高重要性边界。简化 Meeus 与 VSOP87 黄经误差约 0.5 角秒量级，**「节气分钟级」与「历书秒级」必须分层表述**，不得合并成一个精度承诺。
- `geo-time`：真太阳时 = 地方真太阳时（均时差 + 经度差 + 时区）；**北京时间 ≠ 真太阳时**；仅在用户给定经度/地点时可用，默认关闭并提示不确定性。
- `referee`：sxtwl 以 CLI/Worker 独立运行，只比对日期、节气、干支，不比对高级占卜语义。
- `calendar`：农历与闰月**不自研**，委托 `lunar-javascript`（锁定 minor 版本 + `LunarAdapter` 适配层）。

**对外精度表述（写进 UI，不得夸大）**：1900–2100 默认设置下与公开历表/参考实现交叉验证；高精度节气时刻仅研究模式；超出已验证年份显式「超出支持范围」。未做全量天文年历逐秒比对前，**不宣称毫秒级**。

### 4.1 测试矩阵

| 测试 | 工具 | 通过阈值 |
|---|---|---|
| 太阳黄经单元测试 | 已知 JDE 样本 | 与公式示例一致 |
| 节气边界 | 自研 vs VSOP/历表抽样 | 普通模式 < 设计阈值；研究模式仅记录差异 |
| 农历 / 节气 | 自研 / lunar / sxtwl | 1900–2100 逐日一致 |
| 真太阳时 | 手工天文公式 | 东经 120° = 区时；经度极值不越界 |
| 日柱 | JDN 取模 | 200 锚点双向无误 |
| 性能 | 1000 盘 | Web/Electron 不卡顿；移动端真机按 §2.7 门槛 |

### 4.2 校准与降级策略

- 干支**日柱**只从 JDN 锚点取模导出，绝不由年柱逻辑推导。
- 节气瞬时差 ≤2 分钟接受；>2 分钟**不硬抛错**（fail-closed 会让边界日期直接不可用），而是标 `confidence:'degraded'`、优先采用外部库值、追加 `calibration-report.jsonl`。
- 流派约定（早/晚子时、立春/正月换年、真太阳时开关）**不算校准失败**，UI 明示当前约定。
- 每个差异进「分歧台账」：`divergence_id` + 复现输入 + 各系统输出 + 配置 + 原始依据 + 处置（接受/待原文/冻结默认）。**禁止从公开站反推算法后写死。**

---

## 5. 案例本与标注系统（★v3，核心增量）

> 数据模型、99 条配额、主动标注、闭环统计、存储备份、导出格式——v3 §5 已定义完整，此处收录关键模型。

### 5.1 数据模型（`ledger/schema.ts`）

```ts
export interface CaseRecord {
  caseId: string;                 // uuid v7（带时间序，便于排序）
  artType: 'bazi'|'liuyao'|'meihua'|'qimen'|'liuren'|'xiaoliuren'|'jinkou'|'ziwei';
  createdAt: string;              // ISO8601（本地时区 + utcOffset）
  schemaVersion: number;

  question: {                     // ★ 来自 intake
    category: CategoryId; subCategory?: SubCategoryId;
    text?: string;                // 用户原文（可选加密）
    structured: { who?; what?; timeRange?; options?; location?; needTiming? };
    summary: string;              // 系统生成的问题摘要，用户确认过
  };

  input: {                        // 可复现输入快照
    raw: RawInput; normalized: NormalizedMoment;
    config: ResolvedConfig; configHash: string; engineVersion: string;
  };

  result: {                       // 引擎产出（不可变）
    chart: unknown; ruleHits: RuleHit[]; warnings: Warning[];
    evidenceRefs: CitationRef[];  // ★v4 升级为完整 CitationRef，可跳转
    boardHash: string;
  };

  interpretation?: {               // AI 精解（可选，默认关闭）
    provider; model; promptHash;
    cards: JudgmentCard[];        // evidenceIds / counterEvidence / confidence
    retrievedIds: string[]; coverageScore: number; createdAt: string;
  };

  annotation: UserAnnotation;     // ★ 主动标注
  status: 'open'|'resolved'|'archived';
  remindAt?: string;               // 回看提醒
  linkedCaseIds: string[];        // 同一事项多次占/追问
  tags: string[];
  revision: number;               // 乐观并发
}

export interface UserAnnotation {
  presetTags: Array<'应验'|'部分应验'|'未应验'|'存疑'|'重要'>;
  customTags: string[];
  outcome?: { result: '应验'|'部分应验'|'未应验'|'无法判断'; at: string; note? };
  rating?: 1|2|3|4|5;
  note?: string;                  // 自由备注（Markdown）
  keyTakeaway?: string;           // 「这次学到了什么」
  matchedRuleIds?: string[];      // 用户认为「断对了」的规则
  updatedAt: string;
}

export interface FeedbackStats {  // 本地闭环统计，只校准解释，不改排盘
  byArt: Record<ArtType, { total; judged; hit }>;
  byCategory: Record<CategoryId, { total; hit }>;
  byRuleId: Record<string, { shown; confirmed }>;
  computedAt: string;
}
```

### 5.2 99 条配额策略

- **每术独立**：八种术数各自 99 条，互不占用；`status='archived'` 不计入 99 条但保留在归档区，可恢复。
- **软硬阈值**：90 条 → 软提醒；99 条 → 引导「归档最旧 N 条 / 导出备份 / 提升上限」，**绝不静默删除**。
- **可配置**：99 / 199 / 无限（无限仅建议桌面）+ 自动归档阈值；移动端默认 99。
- **容量**：99×8 ≈ 792 条，单条约 10–60KB（AI 精解另计），总计约 8–48MB，IndexedDB 无压力；附件单独存。
- **去重**：同一 `configHash` + 同一 `question.summary` + 5 分钟内视为重复起卦，提示「覆盖/另存」。

### 5.3 主动标注交互

| 入口 | 交互 |
|---|---|
| 出盘后立即 | 底部「记录并标注」条：一键存档 + 预设标签 + 一句话 |
| 记录本列表 | 左滑/右键：标注、备注、关联、提醒、导出、删除 |
| 标注面板 | 结果反馈（含时间）、评分、关键收获、命中的规则、自定义标签、自由备注 |
| 回看提醒 | 「3 天 / 7 天 / 30 天后提醒」→ 系统通知（§2.4 通知权限） |
| 列表筛选 | 按术数/事项/标签/结果/时间/AI 精解/证据，支持全文搜备注 |
| 同事项串联 | 「同一件事再占一次」自动 `linkedCaseIds` 关联 |

### 5.4 本地闭环统计（只校准解释，不改排盘）

按术数/事项/规则/用神维度计算「用户确认命中的比例」。**红线**：只提醒用户自己的解释习惯（如「你在『六爻·求财』过度依赖财爻持世，确认率仅 30%」），**绝不回写排盘层、绝不调整规则权重、绝不改变盘面输出**。样本 <20 条时显式标注「样本不足，仅供参考」。

### 5.5 存储、迁移、备份与导出

- **存储**：Dexie，`cases/annotations/attachments/feedbackStats/archive/meta` 表；统一三壳。
- **写入**：事务化；起卦完成即写盘（不等标注）；崩溃恢复：启动时扫 `status='open'` 且无完成标记的条目，提示「上次有未完成的起卦」。
- **迁移**：`schemaVersion` + 迁移函数链；破坏性变更必须先导出。
- **备份**：全量 `.xuan-case.json`（含 `exportedAt/appVersion/checksum`）；导入按 `caseId` 合并，冲突保留较新者（可选「保留双方」）。
- **隐私**：默认不上传、不云同步；将来做同步须显式开关 + 加密 + 明确告知。
- **导出格式**：`.xuan-case.json`（完整）/ CSV（摘要统计）/ Markdown（含盘面+断语+出处+反馈，★v4 可选带 Text Fragment 链接）。

---

## 6. 指引排盘 · 断事路径卡 `playbook`（★v4 核心，含四张完整示例）

### 6.1 为什么「指引」比「推荐」更重要

同一张六爻卦，问「东西丢哪了」和「身份证是不是被人拿走了」，取用神完全不同（前者看物类用神，后者参看官鬼辨盗）。模型不知道你在问什么，就只能说最泛的话。

v3 解决了「问得准」（intake）与「算得准」（规则+ruleId）。v4 补上中间一环：**「路径准」**——告诉用户（和系统）这件事完整的断法路径。

### 6.2 `Playbook` 数据模型（九段）

```ts
export interface Playbook {
  id: string;                        // 如 liuyao.lost.v1
  category: CategoryId; subCategory?: SubCategoryId;
  version: number;

  arts: {
    primary: ArtType;
    alternates: Array<{ art: ArtType; reason: string }>;
    whyPrimary: string;
  };

  howToAsk: {
    goodExamples: string[]; badExamples: string[];
    requiredFields: Array<'who'|'what'|'timeRange'|'options'|'location'|'needTiming'>;
    clarify: ClarifyQuestion[];      // 证据不足时的澄清追问
  };

  howToCast: CastingGuide;           // 起卦/排盘操作指引

  yongShen: Array<{                  // 取用神（按物类/人事分列）
    condition: string; yongShen: string;
    ruleId: string; citations: CitationRef[];
    confidenceLevel: 'A'|'B'|'C'|'D';
  }>;

  signals: Array<{                   // 看什么信号
    name: string; meaning: '吉'|'凶'|'变数';
    ruleId: string; citations: CitationRef[];
    confidenceLevel: 'A'|'B'|'C'|'D';
  }>;

  locating?: {                       // 定方位/取象（失物/出行/寻人必需）
    byGuaGong?: string;
    byYaoWei?: Record<number, string>;
    byDiZhi?: Record<string, { dir: string; places: string[] }>;
    byLiuShen?: Record<string, string>;
    ruleId: string; citations: CitationRef[];
    confidenceLevel: 'A'|'B'|'C'|'D';
  };

  timing: {                          // 断应期
    rules: Array<{ name; ruleId; citations; confidenceLevel }>;
    fallback: string;                // 无规则时话术
  };

  readingList: Array<{               // ★ 读哪本书（与 reader 打通）
    canonicalId; book; chapter; segIds?: string[];
    why: string; priority: 1|2|3;
  }>;

  forbidden: string[];               // 禁用项
  disclaimer: string;                // 免责话术

  recordTemplate: {                  // 记录模板（喂给案例本）
    fields: Array<{ key; label; type:'text'|'number'|'date'|'enum'; options? }>;
    hint: string;
  };
}
```

**关键约束**：`yongShen/signals/locating/timing` 每条必须带 `ruleId + citations + confidenceLevel`。**无原典出处的民间口诀一律标 D 级「流派说法」，与 A/B 级分区展示，绝不混排。**

### 6.3 通用起卦操作指引 `CastingGuide`

| 起卦法 | 操作指引 | 适用 |
|---|---|---|
| **摇卦（铜钱/硬币）** | ① 净手静心 ② **专念一事**（默念 1–3 遍，勿杂念）③ 双手合扣摇动掷于桌面 ④ 记三正/二正一反 ⑤ 连摇六次，**自下而上**成卦 ⑥ 六次必须同一事 | 六爻 |
| **报数** | ① 临时起念，**勿事先想好数** ② 报 2–3 个数 ③ 按先天卦数/爻动成卦 | 六爻、梅花 |
| **时间卦** | 以问事时的年月日时（或分秒）起卦；**真太阳时开关先确认** | 梅花、六爻、奇门、六壬 |
| **字占** | 报 1–2 汉字，按笔画或字形起卦；繁简体需明示 | 梅花 |
| **手动指定** | 直接选卦名/爻变，用于**复盘与验卦**（学习场景） | 六爻、梅花 |
| **正时/活时** | 正时＝问事当时；活时＝报数或抽签定时 | 大六壬、奇门 |

**通用提示**：一事一卦；**不因卦不吉就重摇**（7 日内同事项重复起卦会提示）；先填事项与问法再起卦——顺序反了取用神会失准。

### 6.4 示例一：失物（用户点名）

| 段 | 内容 |
|---|---|
| **术数** | 主：**六爻**（断一物之所在、可寻与否、方位应期）。备：**小六壬**（速断可否）／**奇门**（方位与外失远近）／**梅花**（万物类象取物形） |
| **怎么问** | ✅「我的身份证昨天下午在地铁站附近丢了，三天内能找回吗」❌「我东西丢了」（不指明何物，取用神会错）。必填：**何物**、**何时何地丢失**、**是否怀疑被盗**、**是否要应期** |
| **怎么起** | 摇卦：净心，专念「某物现在何处、能否寻回」，连摇六次；或报数、时间卦。**一事一卦，不因第一卦不吉重摇** |
| **取用神** | 妻财爻：现金/钱包/首饰/手机/钥匙/电子产品〔`liuyao.lost.yongshen.caifu`〕；父母爻：证件/合同/车票/书本/车/衣物/眼镜〔`…fumu`〕；子孙爻：宠物/玩具/活物/药品〔`…zisun`〕；兄弟爻：普通衣物鞋帽/共享物品〔`…xiongdi`〕；**官鬼爻：通用代指小偷/扣押/风险**，疑被盗时为辨盗关键〔`…guigui`〕；世爻：求测人〔`…shiyao`〕；内/外卦：内卦本宫＝家中未远、外卦他宫＝已流向外〔`liuyao.lost.neiwai`〕 |
| **看什么** | **吉**：用神旺相、不空不破不墓绝、生合世爻或持世 → 可寻〔`…ji.wangxiang`〕；子孙旺动（福神制鬼、有人送回线索）〔`…ji.zisun`〕；用神安静在内卦 → 原地未动只是被遮〔`…ji.neijing`〕。**凶**：用神旬空化空、月破日绝、无原神生扶 → 难寻〔`…xiong.kongpo`〕；用神动化退神/化鬼/化死墓绝 → 变卖丢弃损毁〔`…xiong.huatui`〕。**变数（先判遗失/被盗）**：官鬼不上卦/旬空/休囚安静/入墓 → 无贼纯健忘〔`…biangui.wuzei`〕；官鬼旺动、多鬼齐动、用神被旺鬼克 → 蓄意偷窃或拾而不还〔`…biangui.youzei`〕 |
| **定方位取象** | **爻位**：初爻地面墙角鞋柜地下室／二爻厨房卫生间床底／三爻卧室客厅矮柜／四爻高柜书桌玄关／五爻吊顶楼道路途中／六爻房顶阁楼远方〔`…loc.yaowei`〕。**地支**：子水正北水边冰箱卫生间／午火正南窗台暖气／卯木正东木门衣架绿植／酉金正西首饰盒化妆台金属筐／辰戌丑未箱柜土堆杂物〔`…loc.dizhi`〕。**六神**：青龙整洁显眼处／朱雀纸张票据堆／勾陈堆叠杂物下被压／螣蛇缠绕夹缝／白虎硬物金属下阴暗处／玄武隐蔽暗处抽屉深处〔`…loc.liushen`〕 |
| **断应期** | 通用铁律：静待冲、动待合、空待出空、墓待冲墓、破待补破、伏待冲飞〔`…timing.general`〕。速（1–3 日）：用神旺静临日建、在内卦 → 冲日/当日/次日；中（4–15 日）：入墓/逢破/伏藏有引拔 → 冲墓/补破/冲飞之日；迟（半月–数月）：在外卦/逢合绊/化退 → 出空/解合之月；无应期：用神空绝化鬼、外卦鬼旺动 → 建议止损〔`…timing.wu`〕 |
| **读哪本书** | ①《卜筮正宗》失物章（取用神「各归其类，物有所主」）②《增删卜易》失物章（用神宜旺不宜空破；鬼动克世之辨）③《黄金策》「物失难寻，凭用神之存亡」④ 可选《易林》类象 |
| **禁用** | ❌ 不得指名道姓断言「某人偷的」（只能说「有外力介入/内卦熟人/外卦外人」倾向）❌ 不得教唆搜查他人身体或住宅 ❌ 贵重物品被盗应提示**报警并保留证据**，卦象不作为法律证据 |
| **记录模板** | 何物 / 丢失时间地点 / 卦象结论（能否寻回、方位、应期）→ **事后回标**：是否找回、实际位置（对照方位）、实际时间（对照应期）、与卦象差异 |
| **可信度说明** | 用神分类、爻位/地支/六神定位在民间传承中高度一致，但**各版本表述有出入**。系统内置时**必须回链原典（A/B 级）**；只能源到现代整理或网络口诀的一律标 **D 级** |

> 注：失物断法有大量口诀汇编（如「寻物先分偷与忘」），可作 D 级补充，但**不得与原典并列而不加标识**。

### 6.5 示例二：姻缘（用户点名）

| 段 | 内容 |
|---|---|
| **术数** | 主：**六爻**（关系走向、对方态度、应期最细）。备：**八字**（合婚、命局婚配、婚运时机）／**大六壬**（人事过程与双方互动）／**梅花**（速断心意） |
| **怎么问** | ✅「我和 A 三个月内能否确定关系」✅「这段婚姻是否还有修复可能（半年内）」❌「他爱不爱我」（主观、不可证伪、易反复占）❌「我的正缘是谁」（无法具体到个人）。必填：**关系现状**（已婚/恋爱/分手/相亲）、**对方是否已知**、**时限**、**诉求**（复合/推进/分手/求证） |
| **怎么起** | 一事一卦；**忌短期内反复摇**（「初筮告，再三渎」），7 日内同事项重复起卦提示先看上一条 |
| **取用神** | 男占：妻财爻为女方/对象〔`liuyao.love.yongshen.nan`〕；女占：官鬼爻为男方/对象〔`…nv`〕；**应爻**：对方/彼方态度〔`…ying`〕；**世爻**：自己〔`…shi`〕；已婚兼看**父母爻**（婚书、家庭）〔`…fumu`〕 |
| **看什么** | **吉**：世应相生相合、用神旺相生世、官财得位〔`…ji.shiying`〕；用神持世、与世比和 → 两情相悦。**凶**：世应相克相冲、用神空破、兄爻（竞争者）旺动〔`…xiong.chong`〕；官鬼动而克世（男占女）→ 有压力/阻碍。**变数**：玄武临用（暗昧/隐瞒）〔`…bian.xuanwu`〕；桃花/咸池神煞；用神化进（推进）化退（转淡）〔`…bian.huajin`〕 |
| **定方位取象** | 用神地支/卦宫看对方方位、结识场景；六神看对方性格倾向（青龙温和、朱雀善言、白虎刚烈、玄武深沉）〔`…loc`〕 |
| **断应期** | 合待冲、冲待合、空待出空、旺待值日值月〔`…timing`〕；用神动而逢值逢合之期 |
| **读哪本书** | ①《增删卜易》婚姻/子嗣章 ②《卜筮正宗》婚姻章 ③ 八字合婚参《三命通会》《渊海子平》相关章节 |
| **禁用** | ❌ 不得预测/暗示具体第三人身份 ❌ 不得给出「必须分手」「必须结婚」行为指令 ❌ 涉及家暴、胁迫、未成年人 → **直接转专业机构指引** ❌ 不得用于窥探他人隐私 |
| **记录模板** | 关系现状 / 诉求 / 卦象结论 → **事后回标**：关系走向（确立/缓和/分手/无变化）、发生时间（对应期）、卦象与实际差异 |

### 6.6 示例三：事业（用户点名）

| 段 | 内容 |
|---|---|
| **术数** | 主：**六爻**（具体岗位/项目成败与应期）。备：**八字**（命局适合什么行业、大运走势，适合「长期方向」）／**奇门**（择时、方位、谈判时机）／**大六壬**（职场人际与博弈过程） |
| **怎么问** | ✅「我投的 A 公司这个岗位，一个月内能否拿到 offer」✅「这个项目下季度能否顺利验收」❌「我事业会不会成功」（太泛，无法取用神与应期）。必填：**具体事项**（求职/升迁/创业/项目/考公）、**目标**、**时限** |
| **怎么起** | 摇卦专念一事；项目/投标类亦可用**时间卦或报数** |
| **取用神** | 官鬼爻：功名/职位/官职/录取〔`liuyao.career.yongshen.guigui`〕；父母爻：**文书/合同/offer/学历/印章/公司资质**〔`…fumu`〕；妻财爻：薪资/利润/生意收益〔`…caifu`〕；兄弟爻：同事/竞争者/分割利润者〔`…xiongdi`〕；子孙爻：下属/创意/技术产出/克官之忧〔`…zisun`〕 |
| **看什么** | **吉**：官父两旺、官鬼持世或生世、父母（offer/合同）旺相不空〔`…ji`〕；财官相生。**凶**：官鬼空破、子孙旺动克官（职场是非、岗位不保）〔`…xiong.zisunkegu`〕；兄弟旺动（竞争激烈、利润被分）。**变数**：官鬼化进（职位推进）化退（转淡）；父母空亡（合同未实/流程卡住）；世爻空亡（自身犹豫/准备不足） |
| **断应期** | 官鬼旺相值日值月、父母出空填实、合待冲〔`…timing`〕 |
| **读哪本书** | ①《增删卜易》求名/求财/文书章 ②《卜筮正宗》仕途章 ③ 八字方向参《子平真诠》《滴天髓》格局与用神 ④ 择时参《协纪辨方书》 |
| **禁用** | ❌ 不得承诺录用/中标结果 ❌ 不得建议违法违规操作（行贿、伪造材料）❌ 涉及裁员、劳动纠纷 → 提示劳动仲裁/法律咨询渠道 |
| **记录模板** | 事项/目标/时限/卦象结论 → **事后回标**：结果（成功/失败/延期）、实际时间、与卦象差异 |

### 6.7 示例四：过去未来（用户点名，**需拆分处理**）

**「过去未来」不是一类事，而是两类能力边界完全不同的问题，必须拆开——否则用户会以为术数能「精确回放」，这是最容易失望的地方。**

#### A. 看「过去」——三条路径，能力递减

| 路径 | 适用 | 怎么做 | 能力边界（直说） |
|---|---|---|---|
| **八字复盘** | 回顾已发生的大运流年，理解「为什么那几年顺/不顺」 | 排出生四柱 → 看已走过的大运与流年 → 对照已知事实 | 只能看**运势趋势与关键节点**，不能还原具体事件细节；需用户自己提供事实对照 |
| **六爻验卦** | 卦已排，用已知事实反推「这卦准不准、用神取对没有」 | 对已发生之事起卦 → 用已知结果校验卦象 | 本质是**校准工具**，帮用户和算法发现取用神/断法偏差，不是「查过去」 |
| **六壬/梅花来意占** | 对方不说明来意，占其为何而来 | 六壬起课（正时/活时）→ 观四课三传与发用推来意；梅花以卦象类推断来意 | **高阶技法，九宗门之外的课体变化大，无公开可核对黄金样本**；系统只提供基础课体与提示，标「资料不足，仅供参考」 |

> 系统提示话术：「本软件对『过去』的作用是**复盘与校准**，不是精确回放。请提供已知事实来对照卦象，才能判断这次取用神与断法是否准确。」

#### B. 看「未来」——两条路径

| 路径 | 适用 | 怎么做 | 能力边界 |
|---|---|---|---|
| **占具体事**（六爻/奇门/六壬） | 「三个月内能否收回货款」「下季度项目能否验收」 | 按对应 playbook 起卦 → 用神旺衰 → 应期推法 | **术数最擅长的领域**：给趋势、成败倾向、时间窗口。给的是**可能性与时机**，不是确定结果 |
| **看运势走势**（八字） | 「未来几年适不适合创业」「哪步大运要注意」 | 排大运流年 → 看五行喜忌与格局变化 | 看**阶段性势**，不宜断某件具体事成败 |

**统一字段**

| 段 | 内容 |
|---|---|
| **术数** | **看过去**：八字（复盘）／六爻（验卦校准）／六壬（来意占，高级、样本不足）。**看未来**：六爻/奇门/六壬（具体事+应期）／八字（大运流年走势） |
| **怎么问** | ✅「2020–2023 这步大运我为什么事业反复」（复盘）✅「未来三年适不适合转行」（走势）✅「这笔货款 Q3 能否收回」（具体事）。❌「我前世是谁」「我三年前那天下午到底发生了什么」（无法验证，不予支持）。必填：**方向**（过去/未来）、**具体诉求**（复盘/校准/趋势/成败/时机）、**已知事实**（看过去时必填，用于对照） |
| **禁用** | ❌ 不得宣称能「精确回放/预知」具体事件 ❌ 不得用于「查他人隐私/前世/因果」❌ 涉及已发生的伤害事件、案件 → 引导至专业机构，不做推断 |
| **记录模板** | 方向/诉求/已知事实/卦象结论 → **事后回标**：实际发生时间、与卦象吻合度（高/中/低）、取用神或断法需要修正的地方 |

### 6.8 引导的即时提示与澄清追问

| 触发 | 提示 |
|---|---|
| 未选事项直接起卦 | 「不填事项也能排盘，但解释会偏泛，推荐花 20 秒选一下」→ 一键进入 playbook |
| 问句缺关键信息（失物未说何物） | 「**取用神需要先知道丢的是什么**——证件取父母爻、钱包取妻财爻，差别很大」 |
| 问句过泛（「我事业会不会成功」） | 「本术长于断具体事件，建议拆成『某岗位/某项目 + 时限』」+ 正例 |
| 同一事项 7 日内重复起卦 | 「同一事重复占会互相干扰（古籍云『初筮告，再三渎』），建议先看上一条记录」 |
| 起卦方式与该术数不匹配 | 明示并给出可用方式 |
| 事项为健康/生育/投资/法律 | 前置免责与专业机构提示，必要时终止确定性表述 |
| 证据不足（KB 未命中） | 显示「此流派/术语暂无内置依据，请导入书库」+ 1–2 个澄清问题（本地模板） |

---

## 7. 五术实现范围与落点

### 7.1 分期落点总表

| 术数 | 期数 | 本期实现 | playbook 覆盖（★v4） | 明确留二期 |
|---|---|---|---|---|
| **八字** | P2 | 四柱（立春/正月初一换年可配、早/晚子时可配）、藏干、十神、纳音、大运起运折算、流年、十二长生、五行旺衰打分、格局法+旺衰法双轨用神、天乙/羊刃/禄/文昌/驿马/桃花/华盖/将星/空亡、刑冲合害会 | 事业（方向/流年）、姻缘（合婚）、过去未来（复盘/走势）、学业、健康（趋势，不给诊断） | 地支暗合细化、盲派断法 |
| **六爻** | P2 | 6 类起卦、京房八宫、纳甲、六亲、世应（查表+生成器双实现）、六神、旬空月破日冲、变卦互卦错综、用神取法、六冲六合三合进退神、伏神飞神、暗动 | **失物/姻缘/事业**（★ 用户点名优先）、求财、学业、出行、官非、决策、健康 | 六亲持世分占全表、六神组合细化 |
| **梅花** | P2 | 年月日时/报数/字占起卦、体用生克、互卦变卦、万物类象、《梅花易数》原文断语 | 决策（速断）、失物（类象）、姻缘（心意）、出行 | 外应占、声字占扩展 |
| **奇门** | P4 | 时家为主：拆补/置闰、阴阳遁、三奇六仪、值符值使、九星八门八神（普通+道家两种可配）、转盘、中宫寄坤/艮可配、暗干、四层盘、格局库、四害标红、星门旺衰、点击宫位出提示、搜局 | 出行（方位吉凶）、择日、求财（时机方位）、事业（谈判/投标时机）、失物（方位） | 飞盘、转飞结合、鸣法、括囊、山向、奇门穿壬、年/月家、御定奇门 |
| **大六壬** | P4 | 月将过宫、正时/活时/报数、天地盘、四课、三传九宗门全、遁干、十二天将、神煞、课体、《毕法赋》断语 | 合作/人际（博弈过程）、官非、过去未来（来意占，标「资料不足」）、姻缘（互动） | 毕法赋全 100 条逐条对号、六壬变课体全量 |
| **小六壬** | P4 | 月→日→时递推 + 六神细断 | 失物（速断）、出行（速断）、决策（可否） | — |
| **金口诀** | P4 | 地分四位 + 五动三动 | 合作、求财、出行 | 全量断法 |
| **紫微** | P2 | 安星引擎委托 `iztro`（锁版本 + `ZiweiAdapter`）：十二宫、十四主星、辅星、四化、五行局、命主身主、大限；流派配置（四化表/亮度表）入 `configHash`；**不自研安星算法**（安星流派差异大，委托成熟库 + 测试锁定；v8 决策台账 D18 将紫微纳入 P2） | 事业（方向/流年）、姻缘（合婚/适配）、过去未来（复盘/走势）、决策 | 飞星派四化全量、中州派特殊安星、四化飞星断法 |

**奇门分支为何分期**：飞盘/鸣法/括囊/山向/穿壬各自盘面结构不同，且无公开可核对黄金样本，硬写只能猜，会把已验证核心盘面拖进不可信区间。接口与 config 分支位预留好。

**playbook 落地原则**
- 先做**六爻的失物/姻缘/事业**（用户点名，且六爻是 P2 主术），三张卡做精做透作为其余 playbook 模板。
- 每张 playbook 的 `yongShen/signals/locating/timing` 每条必须有 `ruleId + citations + confidenceLevel`；**原典引用优先从已导入语料取，无法回链原典的民间口诀一律 D 级**。
- 奇门少数派分支、六壬变课体全量——缺公开可核对样本，playbook 相应条目标「资料不足」，**不硬写**。

### 7.2 配置矩阵与验证样本（P2 三术）

| 术数 | 配置项（默认/可选） | 验证样本 |
|---|---|---|
| 八字 | 换年：立春/正月初一；换月：节气（五虎遁）/农历初一；子时：23:00 切分（日柱次日）/夜子时/自定义；真太阳时：关/开；旬空：常规/截路空亡；大运：顺逆按阴阳，起运近似/精确分钟 | 1900-02-04、1984-02-02、2000-02-04 边界；各节气 ±1 分钟；闰月前后；23–1 时；经度跨越；非 +08 时区 |
| 六爻 | 世应：查表/生成器；月破条件；暗动条件；伏神得出条件；旬空填实 | 六静爻、一至六动、伏神、月破、旬空、反伏吟、进退神 |
| 梅花 | 动爻规则；体用定义；互卦变卦开关 | 时间/数字/手动；一至多爻动；体用互变 |

**黄金样本最小规模**：八字 **200 例**、六爻 **150 例**、梅花 **100 例**、紫微 **100 例**（覆盖闰年、闰月、节气同日、跨日、23–1 时、经度跨越、无经度、非 +08 时区等）。奇门/六壬/小六壬/金口诀 P1 只建 fixture 骨架，P4 各自补齐。紫微样本以 `iztro` 输出为机械字段基线，另与公开排盘站抽样互证。

**对古籍体例差异不伪装成唯一真理**：保存 `ruleId`（如 `buceshizong.yuepo`），并以《增删卜易》《卜筮正宗》原文并列呈现。梅花与六爻**共享八卦与爻对象，绝不复用同一吉凶判定器**。

---


## 8. 知识库（v3 §8 保留 + v4 为阅读器重构语料组织）

### 8.1 语料与版权分层

内置语料按**原始证据**组织，每条含：`canonical_id / title / author / edition / publication_date / source_url / access_date / license / page·chapter / transcription_confidence / segment / normalized_text / annotations`。

- **可纳入内置**：公有领域原本 / 明确开放许可文本（维基文库《增删卜易》等原本，及识典古籍版本对照转录）。
- **不可纳入内置**：现代点校本、注释本、OCR 全文库、教材、商城 PDF——「古籍无版权」**不等于**标点/校勘/注释/数据库权利自由；仅允许用户本地导入。
- **用户导入**：默认标记 `user-owned`，**不云同步、不再分发**；导入页提示「请确保你拥有该文件或获得授权」，采用白名单导入而非预判合法性。

初版精选 **600–1200 条**可溯源条目，覆盖《增删卜易》《卜筮正宗》《滴天髓》《子平真诠》《毕法赋》《六壬指南》《梅花易数》《烟波钓叟歌》《协纪辨方书》；每条可溯源，缺口由抓取/导入补。

### 8.2 语料格式扩展（★v4 新增硬要求：章节层级）

```
data/.kb/books/<canonicalId>/corpus.jsonl
每条字段（★ 为 v4 新增）：
  canonical_id / title / author / edition / publication_date
  source_url / access_date / license
  volume ★ / chapter ★ / section ★ / seq / segId ★     ← 章节层级（生成目录树 + 段级定位）
  text / normalized_text / charRange ★                  ← 段内定位所需
  tags[] / annotations
  transcription_confidence / isPublicDomain
  confidence_level ★                                    ← A/B/C/D 分级
```

**导入管线改造**：txt/md/epub/pdf 导入时，除切分外必须额外产出 `volume/chapter/section/segId`，并**在切片时固化 `charRange` 与校验和**。OCR 文本（影印本）因坐标缺失，`charRange` 置信度需下调并在 UI 提示「转录可能不准」。

### 8.3 检索（v4 增强）

- **第一阶段**：可解释 BM25——CJK unigram + bigram 分词（`一二三` → `一二`、`二三`）+ 手工术语同义词词典（旬空/月破/世应/飞伏/三传四课/进退神/体用互变/遁甲直符值使…），`idf = ln(1 + (N - df + 0.5)/(df + 0.5))`，`k1/b` 可调，全部参数入索引版本。同义词只做查询扩展，不做语义替代。
- **第二阶段**：本地 embedding 藏在同一 `Retriever` 接口后，**默认关闭**；低端安卓冷启动/内存出问题即延后。
- **存储**：Dexie 统一三壳（IndexedDB）；索引 `documents/sections/terms/postings/synonyms/citationEdges`；Web Worker 解析 + 事务批量写入 + 进度恢复；超 10 万段分片并在低端机实测。
- **★v3 与事项联动**：`intake` 事项分类与关键因子作为**查询扩展词**注入检索（如「求财」→「财爻、妻财、子孙、求财、谋财」）。
- **★v4 playbook 联动**：playbook 的 `readingList` 与 `citations` 预取相关段落，作为该类事项「常备依据」，显著提升命中与跳转成功率。
- **★v4 精确串匹配通道**：新增，用于引文定位与校验（引文必须与原文逐字对得上）。
- **反查索引 `citationEdges`**：段 → 引用它的卦例/规则/判断卡，支撑 §9.5 反向跳转。

### 8.4 导入管线

| 格式 | 方案 | 要点 | 验证 |
|---|---|---|---|
| txt / md | 原生 File API，行/章节正则切分 | YAML frontmatter | 字符数、空段率 |
| epub | JSZip + OPF/NCX 解析 | 章节顺序、HTML 去标签 | 对照 Calibre 目录 |
| pdf | PDF.js 文本层（Worker 内） | 双栏、页眉页脚、表格后处理 | 人工抽检抽取文本 |
| docx | Mammoth | 标题/脚注映射 | 与 Word 大纲对照 |
| 影印 / 图片 PDF | OCR 不默认启用 | 需用户授权 + 逐页校对 | 字符错误率抽检 |

### 8.5 缺口自填

每次生成精解记录 `query / retrieved_ids / coverage_score / unsupported_claims / human_review_status`。规则：盘面事实只允许来自计算层；经典解释只在至少一条已导入原文命中后展示；**无证据则显示「此流派/术语暂无内置依据，请导入书库」**。

流程：`evidence()` 声明 miss → 写 `kb-gaps.jsonl` → `npm run kb:fill`（桌面/Node）生成检索词 → 从**用户配置的 allow-list 域名**抓正文 → 清洗去重（归一 hash）→ 产出待审卡片到 `data/.kb/inbox/` → UI 一键采纳入库。**绝不静默改写主库**。

**★v4 缺口卡片新增字段**：① 章节路径（用于生成目录）② 可信度初判（来源站点类型 → 建议分级，人工确认）③ 是否疑似现代整理本（若是默认标 D 级，**不得**进入 A/B 级语料）。

---

## 9. 典籍查阅器 · 批注溯源跳转（★v4 核心补充二）

### 9.1 引用模型 `CitationRef`（一切跳转的基础）

```ts
export interface CitationRef {
  canonicalId: string;        // 典籍唯一 ID（如 zengshanbuyi.ws.1912）
  book: string;               // 书名
  author?: string;
  edition: string;            // 版本（如「民国三年校经山房石印本」）
  chapter: string;            // 章节
  segId: string;              // 段落 ID（如 zsby.3.12）—— 定位到段
  charRange?: [number, number]; // 段内字符区间 —— 定位到字（★ 高亮用）
  quote: string;              // 引文原文（冗余存储，脱机可显示）
  page?: string;              // 页码/叶码
  license: string;            // 公有领域 / 用户自有 / 未知
  sourceUrl?: string;
  accessedAt?: string;
  confidenceLevel: 'A'|'B'|'C'|'D'|'E';   // ★ 五档可信度
  transcriptionConfidence?: number;         // 转录置信度（OCR/录入质量）
}
```

**存储**：语料 `corpus.jsonl` 的每条扩展为**保留章节层级**（v4 硬要求）。`segId` 由「典籍 ID + 章节序号 + 段序号」生成，**不随内容编辑而变**；内容修订只更新 `text` 与 `charRange`。

### 9.2 定位与跳转机制（三层）

| 层 | 机制 | 实现 | 适用 |
|---|---|---|---|
| **L1（主）** | 自研段内 `charRange` 定位 | 阅读器按 `segId` 找 DOM → 按 `charRange` 用 Range API 包裹 `<mark>` → `scrollIntoView({block:'center'})` → 300ms 呼吸高亮 | **App 内所有跳转**（三壳一致、离线可靠） |
| **L2（增强）** | Text Fragments `#:~:text=` | 仅**导出/分享**场景生成外链 | 对外分享，非主路径 |
| **L3（P6）** | IIIF Canvas + `#xywh=` | 影印本/扫描件：Canvas URI + `xywh` 区域高亮原页 | 接入扫描件后 |

**L1 可靠性设计（主机制，必须做好）**

- **段 ID 稳定**：`segId` 不随内容编辑而变；`charRange` 由导入管线切片时固化并写校验和；校验失败（文本已改）**降级为「高亮整段」+ 提示「引文位置可能有微调」**，而非跳转失败。
- **异体字/缺字容错**：先按 `segId` 定位段（不依赖文本匹配），再按 `charRange` 取字。
- **深链协议**：内部路由 `xuanshu://read/{canonicalId}?seg={segId}&hl={start}-{end}&from={caseId|ruleId}`；Web 等价为 `#/read/{canonicalId}?...`。`from` 用于**反查来源**。
- **跨壳一致**：Electron / Android WebView / Web 全走同一套 `reader.locate()`，不依赖浏览器原生能力。

**L2 使用限制（写进代码注释）**：仅检测到 `window` 为主框架且浏览器支持时，才在「复制链接」里附加 Text Fragment；**UI 不提示、不依赖**。中文古籍片段优先 `prefix-,textStart,textEnd` 语法提高命中率。

### 9.3 阅读器功能

| 模块 | 功能 | 说明 |
|---|---|---|
| **书架** | 典籍列表：书名/作者/版本/条目数/进度/来源与许可 | 内置（公有领域）+ 用户导入（`user-owned`，不云同步） |
| **目录树** | 卷 → 章 → 节 → 段 | 语料必须带章节层级才能生成 |
| **正文视图** | 原文（繁体竖排可选）/ 注疏（折叠）/ 白话译（折叠，仅 C 级标注） | 默认只显示原文，避免「译本当原文」 |
| **批注面板** | 用户划词 → 写笔记 → 自动带 `CitationRef` | 批注存本地，**原文与批注分离存储**，导出时可合并 |
| **全书搜索** | 关键词 → 命中列表 → 点击定位并高亮 | BM25 + 精确串匹配双通道；异体/繁简归一后检索 |
| **版本对照** | 同一条文多版本并列 | 差异显式标注，**不自动判定优劣** |
| **书签/进度** | 书签、最近阅读、进度百分比 | 存 `ReadingProgress` |
| **跳转与返回** | 从断语跳入 → 顶部「来自：某卦例/某规则」→ 一键返回 | `from` 参数驱动 |
| **反查** | 某段原文点「被引用」→ 列出引用过这段的所有卦例/规则/判断卡 | ★ 反向链接，把书读厚 |

### 9.4 批注溯源与五档可信度

**三类批注，统一带 `CitationRef`**

| 批注来源 | 生成方式 | 可信度 | 展示 |
|---|---|---|---|
| **系统断语**（规则命中） | `rules()` 输出 `RuleHit`，每条带 `ruleId + citations` | A/B/C/D 按来源定 | 「原典」/「注疏」/「现代整理」/「流派说法」角标 |
| **AI 判断卡** | 检索命中 + 模型组织，必带 `evidenceIds` | **一律 E 级**（除非引用了 A–D 级条目） | 「AI 生成，未经原典核实」；无证据即报缺口 |
| **用户笔记** | 阅读器划词批注 / 案例本备注 | — | 标注「我的批注」+ 时间 |

**UI 呈现规则（硬性）**

- 每条批注后跟**出处角标**：`〔原典·增删卜易·卷三·失物章 ↗〕`，角标颜色按可信度分级（A 绿 / B 蓝 / C 灰 / D 橙 / E 红）。
- 点击角标 → 跳转阅读器并高亮原文；鼠标悬停 → 浮层显示引文全文 + 版本 + 页码。
- **D 级与 E 级必须与 A/B 级在空间上分区展示**，不得混排在「古籍依据」区块内。D 级区块标题「流派说法（非原典）」，E 级「AI 推断（需核实）」。
- 无 `citations` 的断语 → 显示「此断语暂无内置原典依据，请导入书库」并落 `kb-gaps.jsonl`。

### 9.5 双向跳转：断语 ↔ 原文

**正向（断语 → 原文）**

```
盘面/断语面板
  └─ 断语：「用神旺相、不空不破，失物可寻」
       └─ 出处角标 〔原典·卜筮正宗·失物章 ↗〕
            └─ 点击 → reader.locate(citationRef)
                 └─ 打开阅读器 → 滚动到 segId → 高亮 charRange
                      → 顶部「来自：卦例 #123 / 规则 liuyao.lost.ji.wangxiang」
                      └─ 用户划词批注 → 批注带同一 CitationRef
                           └─ 返回 → 批注出现在原断语下方「我的批注」
```

**反向（原文 → 断语）**

```
阅读器 · 《卜筮正宗·失物章》某段
  └─ 点「被引用 (3)」
       └─ 列出：卦例 #123（2026-08-20 占失物）、规则 liuyao.lost.ji.wangxiang、AI 判断卡 #45
            └─ 点击 → 跳回该卦例并高亮对应断语
```

**这就是「把书读厚」的闭环**：读原典 → 划词批注 → 下次排盘命中该段时，你的批注随原文一起出现在断语里 → 读过的书直接变成你的解释能力。

### 9.6 版本对照（轻量版，不做自动判定）

数字人文领域共识：自动校勘能提效，但**最终取舍仍须专家参与**，且版本差异本身可能蕴含重要文献信息。故 v4 做法：

- 同一条目多版本/多出处并列展示，逐字差异用下划线/颜色标注。
- **系统不判定哪个「对」**，只提供：版本信息（刊刻年代、底本、整理者）、差异列表、「查看全部版本」。
- 用户可在阅读器标记「我采用此版本」，该选择随 `configHash` 记录，保证可复现。
- **留二期**：自动校勘（差异自动识别、异文聚类）与版本源流谱系，需先积累足够多版本语料。

### 9.7 与案例本的联动

- 案例本的 `result.evidenceRefs`（v3 定义）升级为完整 `CitationRef[]`，支持跳转。
- 案例详情里每条断语都可跳原文；**用户在某卦例下的批注若关联了 `CitationRef`，会同步出现在阅读器对应段落的批注面板**，并标注来自哪个卦例。
- 导出 Markdown 个案时，**出处可选两种形态**：① 纯文本引用（默认）② 带 Text Fragment 的链接（仅典籍有 `sourceUrl` 且用户勾选「生成在线链接」）。

### 9.8 引用校验 `verify-citation.mjs`

语料更新（导入新书、修订文本）后**必须**运行：

```
$ node scripts/run.mjs verify-citation
检查项：
  ① 每条 CitationRef 的 canonicalId / segId 是否存在
  ② charRange 是否越界、对应文本是否与 quote 一致（允许异体字差异，记录告警）
  ③ 失效引用的降级策略：→ 高亮整段 +「引文位置可能有微调」
输出：citation-report.jsonl
```

---


## 10. AI 精解接入（用户自带 Key，默认关闭）

### 10.1 ProviderRegistry

每项含 `id / displayName / baseUrlTemplate / modelListUrl / authHeader / defaultHeaders / testConnection / transformRequest / capabilityProbe / pricingUrl / docsUrl / retrievedAt`。

**模型名、区域、价格、结构化输出能力持续变化 → 首次连接必须请求官方 model/capabilities 清单探测，不得硬编码成销售承诺。**

| 厂商（检索日 2026-08-29） | 官方 baseUrl | 兼容事实 | 备注 |
|---|---|---|---|
| DeepSeek | `https://api.deepseek.com` | OpenAI 格式，`/chat/completions`，支持 stream | 模型名以官方清单为准 |
| 火山方舟 Doubao | `https://ark.cn-beijing.volces.com/api/v3` | 兼容 OpenAI SDK | stream / JSON 需探测 |
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4/chat/completions` | OpenAI Chat Completions，支持 stream | JSON mode/工具调用需探测 |
| Kimi / Moonshot | `https://api.moonshot.cn/v1` | 兼容 OpenAI，可用 OpenAI SDK | 按模型测能力 |
| 腾讯混元 | `https://api.hunyuan.cloud.tencent.com/v1` | OpenAI 兼容 | 官方说明逐步迁移，查实时模型/价格 |
| 通义千问 DashScope | `{WorkspaceId}.{region}.maas.aliyuncs.com/compatible-mode/v1`（旧：`dashscope.aliyuncs.com`） | Qwen 兼容 Chat，支持 stream | 以官方定价页为准 |
| 百度千帆 | **未从官方页面确认 baseUrl** | **不作已确认项** | 用户实测后自行填自定义端点 |
| 自定义 | 用户填 baseUrl / Key / model | 发送前校验 URL scheme、路径、CORS/代理能力 | 兼容 Ollama / vLLM / 自建网关 |

### 10.2 提示工程：禁止算盘 + 事项约束（v3 增强 + v4 引用纪律）

> 你是玄枢引用助理。只根据提供的**结构化盘面 JSON**、**事项上下文**和**检索片段**输出；**绝不自行推算四柱、干支、节气、世应、纳甲、三传、互变**。若盘面 JSON 与引用冲突，以盘面 JSON 为准并在「证据不足」中说明。每条判断卡必须列出支持证据与反驳证据；无检索依据的论断写入 `unsupportedClaims`。答复必须遵循事项模板（结论→依据→关键信号→应期→反证→建议），不得跳步、不得泛化、不得给出医疗/投资/法律的确定性结论。

**★v4 新增引用纪律**：你只能引用**已提供的检索片段**，并逐字复制其 `quote` 字段，附上 `canonicalId` 与 `segId`。**不得凭记忆写出任何古籍原文**。若片段不足以支撑论断，在 `unsupportedClaims` 中说明并留空 `evidenceIds`——**这优于编造一条看似合理的引文**。

请求载荷 = 结构化盘面 JSON（含 `method/configHash/pillars/hexagram` 等）+ **事项上下文**（category、问题摘要、关键因子、应期需求）+ 检索原文片段（带 `canonical_id`）+ 输出契约；`temperature` 低温。

### 10.3 输出判断卡片（v4 扩展字段）

```
claimId / type（格局·旺衰·用神·卦象·应期·风险提示）/ text
evidenceRefs[]（完整 CitationRef）/ confidence / counterEvidence
timingCandidates / confidenceLevel:'E' / citationVerified:boolean / needsHumanReview
```

若模型不支持结构化输出：先用 JSON tool/function schema + 严格正则修复，`attempt ≤ 2`；失败即降级为「原文摘录 + 人工解读」，**不渲染不可信 JSON**。

### 10.4 密钥与隐私

- **Electron 主进程**：`safeStorage`（Windows=DPAPI，用异步 API）；Key 明文不写 config JSON，主进程内存短留，UI 经一次性 token/事件取用。
- **Android**：`EncryptedSharedPreferences`（AndroidX Security，Keystore + AES）。⚠️ Capacitor `Preferences` **只是轻量 KV，不能存密钥**。
- **调用路径**：Electron 主进程或 Capacitor 原生层代理，不把 Key 放进渲染进程 `localStorage`（同时解决 CORS、请求日志、Key 生命周期）。
- **数据最小化**：本地默认不发送姓名/出生地点/问题原文；联网精解前显示预览、目的与保留策略；提供「匿名化盘面」开关（只保留抽象结构）。
- **审计日志**：`{time, provider, model, inputHash, outputHash, latency, tokens, schemaVersion}`；完整输入输出需用户主动开启调试日志。
- **隐私页明示**：AI 提供商按自身政策处理请求；玄枢不承诺端到端零留存；使用自有 Key 仍受该提供商隐私政策约束。

### 10.5 敏感问题拦截（与事项系统联动）

医疗、心理、孕产、未成年人、投资、法律、刑事等领域，在 **intake 阶段即拦截**，直接屏蔽确定性表述并给出专业机构指引，AI 层再兜底一层。

---

## 11. 分期计划（P0–P6，合并 v3 骨架 + v4 工期调整与验收门）

> 工期按「单人全职投入」估算；兼职或多人并行可按表格拆分。每期都有**明确不做**，防止范围蔓延。

### P0 · 工程地基与工具链（2–3 周）

**目标**：仓库、工具链、三壳最小可跑，平台环境体检通过。

| 交付物 | 内容 |
|---|---|
| Monorepo | npm workspaces；`.npmrc`（npmmirror）+ lock 提交；`.env.node`（Node 绝对路径） |
| 工具链脚本 | `run.mjs`（统一 Node 前缀）、`doctor.mjs`（环境体检） |
| 三壳 Hello World | `apps/web`（Vite+React）、`apps/desktop`（Electron，Win11 起窗）、`apps/android`（Capacitor 8，targetSdk 36，debug APK 可装） |
| 平台骨架 | `PlatformAdapter` 接口 + 三壳实现占位；`apps/ios` 存根 |
| 前瞻轨 | 分支 `android-17-preview` 建好，Capacitor 9 + target 37 能编译 debug APK |

**验收门**：`npm run doctor` 全绿（Node 绝对路径、JDK 21、`ANDROID_HOME`、licenses、`adb devices`、Gradle 8.14.3、磁盘）；`build:web` 成功；Electron 在 Win11 起窗；`cap sync android` + APK 装到 Android 16 真机；前瞻轨可编译。

**明确不做**：术数业务、知识库、阅读器、playbook。

### P1 · 历法内核 + 插件框架 + 案例本 MVP + **playbook 骨架 + reader 骨架**（4–5 周）★ 较 v3 延长

**目标**：可复算地基 + v3/v4 新产品骨架 + **断事路径卡与典籍查阅器最小闭环**。

| 交付物 | 内容 |
|---|---|
| 历法内核 | `astronomy / solarTerms / ganzhi / lunar(LunarAdapter) / solarTime / calibrate` + 双跑与分歧台账 |
| 插件框架 | `ShuPlugin` 契约 v5（含 intake/answer/playbook 声明位）、`registry`、`board/schema`、fixture runner |
| 案例本 MVP | `ledger`：CaseRecord schema、Dexie、**99 条/术配额与软提醒**、标注面板、导出 `.xuan-case.json` |
| **★ playbook 骨架** | `Playbook` 模型 + 三张先行卡（六爻·失物/姻缘/事业，用户点名）+ 6 步向导融入九段路径 |
| **★ reader 骨架** | `CitationRef` 模型 + 语料章节层级改造 + **L1 charRange 定位打通**（断语→点出处→阅读器高亮原文，最小闭环） |

**验收门**
- 节气/干支/真太阳时黄金样本一致；1900–2100 逐日 diff 无未解释分歧。
- 案例本：99 条配额、软提醒、归档恢复、导出导入合并。
- **playbook**：三张卡能在向导里走通「选事项 → 看路径 → 取用神提示 → 读哪本书 → 记录」。
- **reader 最小闭环**：至少 1 本典籍（如《增删卜易》公有领域本）完成章节层级入库；从某条断语点出处能跳到该章并高亮对应文字；`verify-citation` 通过率 100%。

**明确不做**：真实术数排盘、BM25、AI、全书搜索、批注、版本对照。

### P2 · 核心三术：八字 / 六爻 / 梅花（6–8 周）

**目标**：三术出盘逐格可核对 + 规则命中带引用 + 与 playbook 打通。

| 交付物 | 内容 |
|---|---|
| 八字 | §7.2 全部机械项 + BoardSpec 渲染 + 配置面板 |
| 六爻 | 6 类起卦 + 世应双实现 + 纳甲/六亲/六神/动变/伏神/互卦错综 + 用神取法 |
| 梅花 | 3 类起卦 + 体用 + 互变 + 万物类象 |
| 盘面 UI | `BoardRenderer`（plate/grid/table/ring）；点击某格/爻弹出「这是什么、为什么」+ 出处角标 |
| 记录与引导打通 | 三术接入案例本（各 99 条）+ intake 推荐配置预设 + playbook 覆盖（六爻：失物/姻缘/事业/求财/学业/出行/官非/决策；八字：事业/姻缘/过去未来/学业；梅花：决策/失物/姻缘/出行） |

**验收门**：三术 fixture 与两家以上公开站机械字段 100% 一致；每条解释可回链 `ruleId + 出处` 并**能跳转**；**★v4 三术 `rules()` 输出必须携带 `citations`**；每术 99 条记录端到端无阻塞；中低端安卓真机 1000 盘不卡。

**明确不做**：奇门/六壬业务、知识库导入管线、真实 AI 接口实测。

### P3 · 知识库 + **典籍查阅器完整功能** + 精准答复深化（5–6 周）★ 较 v3 延长

**目标**：可溯源知识库 + 装配式精准答复 + **阅读器完整版（批注/反查/版本对照）**。

| 交付物 | 内容 |
|---|---|
| 语料 | 600–1200 条可溯源条目（带章节层级）、`meta.json`（版本/许可/来源/访问日期） |
| 检索 | BM25（CJK bigram + 同义词）+ **精确串匹配** + 事项联动 + playbook 联动 + Dexie/Web Worker |
| 导入管线 | txt/md/epub/pdf/docx；章节层级保留；OCR 不默认启用；质量评分 |
| 缺口自填 | `kb-gaps.jsonl` + `kb:fill`（桌面，白名单域名）+ inbox 待审卡片（**★ 含章节层级与可信度初判**）+ 一键采纳 |
| **★ 阅读器完整版** | 书架、目录树、正文分层（原文/注疏/白话）、**用户批注（划词+CitationRef）**、全书搜索（命中定位）、**反向跳转（段→卦例/规则）**、书签与进度 |
| **★ 版本对照** | 并列 + 差异标注，**不自动判定** |
| `verify-citation.mjs` | 纳入常规流程 |
| 答复装配 | `answer`：各术模板、`AnswerComposer` 四层、应期规则库（ruleId 化）、安全拦截 |

**验收门**
- 100 条术语 Recall@5 达标；未知术语 100% 显示缺口；导入 50/200/1000 文档不阻塞主线程。
- **阅读器**：低配安卓真机长章节滚动与高亮流畅；跳转成功率 100%（失效按 §9.2 降级）；批注可写、可随断语回显；反向跳转可用。
- **引用分级**：全部条目标注 A–E 级；D/E 与 A/B 分区展示；`verify-citation` 通过率 ≥99%（余者降级告警）。
- 全部事项分类均有答复模板；每条答复含「依据（原文+出处）/ 关键信号 / 反证 / 免责」；无证据不生成结论；应期候选全带 `ruleId`，无规则输出「暂无内置应期推法」。

**明确不做**：embedding 默认开启、云端账号、自动算命、自动校勘。

### P4 · 扩展四术：奇门 / 大六壬 / 小六壬 / 金口诀（每术 2–4 周，可并行）

**目标**：同一插件框架接入剩余四术，**每术独立 sprint、独立验收**，**每术配套 playbook**（§7.1）。

| 术数 | 交付 | 验收 |
|---|---|---|
| 小六壬（先做，最简） | 月→日→时递推 + 六神细断 + 接入「速断」通道 | fixture 全过；速断事项（失物/出行/可否）端到端 |
| 奇门（转盘） | §7.1 转盘 + 拆补/置闰 + 格局库 + 四害标红 + 点击宫位出提示 + 搜局 | fixture 与公开站逐格一致；九宫格在折叠屏/平板自适应 |
| 大六壬 | 月将过宫 + 九宗门 + 天将 + 遁干 + 神煞 + 课体 + 毕法赋断语 | 九宗门全量 fixture；涉害/贵人取法分歧进台账 |
| 金口诀 | 地分四位 + 五动三动 | fixture；与六壬共享天地盘对象但独立判定器 |

**验收门（每术）**：独立 fixture 集；与至少一家公开站逐格核对；流派分歧台账；无黄金样本的门派解释标「资料不足」；案例本自动接入（各自 99 条配额）；intake 推荐术数范围同步扩展；**playbook 每条规则带 `citations`**。

**明确不做**：跨术强行统一解释；奇门少数派分支（飞盘/鸣法/括囊/山向/穿壬）与六壬变课体全量（留二期，接口预留）。

### P5 · 三壳完善与上架（4–6 周）

**目标**：Win11 桌面体验、Android 16/17 双轨、合规与上架材料全部就位。

| 交付物 | 内容 |
|---|---|
| Electron 桌面 | Win11 特性（Mica、Snap Layouts、系统主题/强调色、通知、JumpList、任务栏进度、`.xuan-case.json` 文件关联）；`safeStorage` 密钥；主进程 AI 代理；备份导出；自动更新+签名 |
| Android 主线 | Capacitor 8 / targetSdk 36 发布包；16KB 对齐校验；国产 ROM 与折叠屏验收；`EncryptedSharedPreferences` 密钥；Android 17 真机「运行」验收 |
| Android 前瞻轨 | Capacitor 9 / targetSdk 37：§2.5 变更逐条验证（重点：大屏自适应、状态自恢复、本地网络权限按需）；真机通过后合并主线 |
| PWA | Service Worker 缓存静态资源 + Dexie 离线知识库（作为 Web 增强，非第三壳） |
| 合规 | 免责声明（首屏/精解前/导出页脚）、隐私页、数据导出与删除、AI 披露、敏感拦截 |
| 上架材料 | Play 商店（target 36 合规、数据表单、内容分级）、国内安卓商店逐个审阅、Microsoft Store（可选 MSIX） |

**验收门**
- Win11 23H2/24H2/25H2 可装可跑；Win10 21H2+ 冒烟通过；更新流程可用（或明示未签名风险）。
- Android 主线：Android 16 真机全流程（起卦→解释→记录→标注→导出→离线）无阻塞；Android 17 真机「运行」验收通过。
- 前瞻轨：附录 A 核对表全部有结论；满足 §2.3 五项条件才合并主线。
- **★v4 新增验收**：阅读器在 Android 16/17 真机与 Win11 上表现一致；**跳转在 Android WebView 中 100% 可用**（这是 L1 自研机制的关键验证，也是不选 Text Fragments 的理由）。
- 合规门全绿：免责、隐私、导出/删除、AI 披露、敏感拦截、Play target API 合规。

**明确不做**：iOS 出包（仅 macOS CI 冒烟）、付费系统与会员。

### P6 · 运营与进阶（持续）

- **个人校准看板**：案例本闭环统计可视化（样本量提示 + 维度下钻）。
- **备份与可选同步**：本地备份、WebDAV/局域网同步（⚠️ 若做局域网发现需 `ACCESS_LOCAL_NETWORK`）；端到端加密，默认关闭。
- **浮动盘面**（Android 17 App Bubbles / 桌面 PiP）：P6 可选。
- **★ IIIF 影印本接入**：Canvas + `#xywh=` 定位，扫描件高亮原页。
- **★ 自动校勘与版本源流**（需积累多版本语料，人工审定）。
- embedding 检索（默认关闭，低端机实测通过才放开）、厂商 provider 自动探测、商业化、社区案例库（仅用户主动分享，含脱敏与授权确认）。

### 里程碑总览

```
P0 地基 ─ P1 内核+骨架+playbook/reader最小闭环 ─ P2 核心三术 ─ P3 知识库+阅读器完整 ─ P4 扩展四术 ─ P5 三壳上架 ─ P6 持续
         │                                      │             │
         Win11+A16跑通                          三术逐格可核对  批注溯源双向跳转
         断语→原文跳转闭环                        99条案例本     引用五档分级
         三张playbook                            playbook全覆盖 版本对照
```

---

## 12. 验证与发布门

### 12.1 端到端场景（每个壳都要走一遍，含阅读器）

1. **引导起卦**：选事项（如失物）→ playbook 展示九段路径 → 细化问法（触发过泛提示）→ 确认术数 → 起卦。
2. **逐格解释 + 溯源**：点某一爻 → 弹出「这是什么、怎么来的」→ **点出处角标 → 跳转阅读器高亮原文**。
3. **批注闭环**：在阅读器划词写批注 → 返回卦例 → 批注显示在该断语下 → 下次排盘命中同段时，批注随原文一起出现。
4. **反向跳转**：阅读器某段点「被引用」→ 列出引用过它的卦例/规则 → 点击跳回。
5. **版本对照**：某条断语存在多版本 → 并列展示 → 差异标注 → 用户选择采用版本（记入 `configHash`）。
6. **证据闭环**：命中断语显示原文与出处；未命中显示「暂无内置依据，请导入书库」并落 `kb-gaps.jsonl`。
7. **缺口回填**（桌面）：`kb:fill` 抓白名单域名 → inbox 卡片（**★ 含章节层级与可信度初判**）→ 一键采纳入库 → 再次查询命中且可跳转。
8. **精准答复**：按事项模板输出（结论/依据/关键信号/应期/反证/建议/免责），应期带 `ruleId`。
9. **案例本**：存档 → 标注 → 回看提醒 → 补结果 → 个人校准统计 → 导出（含出处，可选 Text Fragment 链接）。
10. **AI 精解**（可选）：判断卡每条证据可跳转；无证据即报缺口；`citationVerified` 为 false 时不显示为已核实。
11. **离线**：飞行模式下排盘、检索、**阅读与跳转**、记录、标注全部可用。

### 12.2 发布 checklist（★ 为 v4 新增）

**通用（继承 v3）**
- [ ] 盘面 JSON 由算法层生成，仓库无 LLM 推导的四柱/卦象
- [ ] `configHash` 一致，任何记录可一键还原
- [ ] 三术 fixture 与两家公开站逐格一致（八字 200 / 六爻 150 / 梅花 100）
- [ ] 免责、隐私、导出/删除、AI 披露、敏感拦截上线

**★ v4 新增**
- [ ] 每类事项均有 playbook；九段完整；「过去未来」已按复盘/验卦/来意 vs 趋势/应期拆分并明示边界
- [ ] playbook 中每条 `yongShen/signals/locating/timing` 均带 `ruleId + citations + confidenceLevel`
- [ ] 民间口诀一律 D 级，与原典分区展示，无混排冒充
- [ ] 全部语料带章节层级（`volume/chapter/section/segId`）
- [ ] **跳转在三壳（Web/Electron Win11/Android WebView）100% 可用**；失效时按 §9.2 优雅降级
- [ ] `verify-citation` 通过率 ≥99%，余者降级告警
- [ ] 引用五档分级全部标注，UI 角标与分区正确
- [ ] 用户批注可写、可随断语回显、可反查
- [ ] 版本对照只并列不判定；用户选择的版本记入 `configHash`
- [ ] 阅读器在低配安卓真机长章节滚动与高亮流畅
- [ ] AI 判断卡的 `evidenceRefs` 均可跳转；未校验的引用显式标注

**平台专项**
- [ ] Android 主线：`targetSdk 36`、16KB 对齐校验通过、Android 16 真机全流程、Android 17 真机「运行」验收
- [ ] Android 前瞻轨：附录 A 核对表全部有结论；满足 §2.3 五项条件才合并主线
- [ ] 大屏/折叠屏：盘面在 sw>600dp 自适应，无拉伸/黑边；不依赖固定方向
- [ ] 预测返回手势与 edge-to-edge 在所有页面正确
- [ ] Windows 11：Mica/主题/通知/文件关联可用；23H2/24H2/25H2 可装；更新验证（未签名需明示）
- [ ] iOS：不承诺 Windows 可构建；仅 macOS CI 冒烟，文档如实写明
- [ ] 案例本：每术 99 条配额、软提醒、归档恢复、导出导入合并全部验证

**已知不可验证项（直说，不包装）**
- iOS 构建与上架（缺 macOS/Xcode）
- 奇门少数派分支（飞盘/鸣法/括囊/山向/穿壬）与六壬变课体全量：缺公开黄金样本
- **六壬「来意占」等高级技法：无公开可核对样本，系统标「资料不足」**
- 真实第三方 AI 接口（缺 Key，用假 endpoint 行为测试替代）
- IIIF 影印本定位（P6，需先接入扫描件语料）

---

## 13. 合规与风险台账（合并 v3 + v4）

### 13.1 免责与敏感拦截

**免责声明（首屏 + 每次精解前 + 导出页脚）**：「本软件提供中国传统术数排盘、古籍检索与文化研究辅助。排盘结果由既定规则计算；解释不构成医疗、投资、法律、人生或行为的确定性预测。请自行理性判断并咨询有资质的专业人士。」

敏感领域（医疗、心理、孕产、未成年人、投资、法律、刑事）在 intake 阶段拦截，输出专业机构指引，AI 层兜底。

### 13.2 商店与审核

- **Google Play**：对健康内容、UGC、知识产权、欺骗行为、隐私、AI 生成均有合规要求；未检索到「占卜禁止」专门条文，但不等于可忽略地区法律 → 保守姿态：不定位医疗诊断、不利用危机/弱势人群营销、不做虚假疗效、完整披露 AI 生成、提供删除与导出。**国内安卓商店逐个审阅，不假设 Play 规则可直接替代。**
- **Apple 审核指南 4.3**：fortune telling 与 dating、flashlight、wallpaper 等并列，除非提供显著不同或改进的体验 → iOS 目标后置，先以 Web/Electron/Android 证明差异化（逐格可验证盘、离线知识库、原文引用、可复现 fixture、可关闭 AI、99 条案例本、事项引导、★v4 批注溯源）后再行动。不复制竞品 UI、图片、案例或数据库。
- **输出定位**：传统历法与文化研究工具，非医疗/投资/法律建议（界面常驻声明）。

### 13.3 风险台账（v3 R1–R10 + v4 R11–R14）

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| R1 | Electron/AGP 依赖联网下载，镜像超时 | 三壳降级为「工程与脚本就绪」 | npmmirror；构建前 `doctor.mjs`；超时即文档化，不影响五术与 Web 交付 |
| R2 | Capacitor 9 仍处 `next` 通道 | 前瞻轨不可按期合并 | 双轨；主线 Capacitor 8 已满足 2026-08-31 合规 |
| R3 | Play target 36 死线已至，P5 出包可能晚于该日 | 新应用无法提交 | 主线本就 target 36；超期走「延期至 2026-11-01」申请或先以 Web/桌面分发 |
| R4 | 国产 ROM WebView 差异导致盘面错位/白屏 | 安卓口碑 | §2.7 真机矩阵；WebView 版本探测 + 降级渲染；不依赖最新 CSS |
| R5 | Android 17 状态自恢复（Activity 不重启） | 配置变更后丢失向导进度 | 状态持久化到 Dexie；不依赖 Activity 重建 |
| R6 | 古籍版权误判（点校本当公有领域） | 法律与下架 | 分层白名单；用户导入标记 `user-owned`、不云同步、不再分发 |
| R7 | AI 幻觉冒充古籍观点 | 产品可信度崩塌 | 无证据即报缺口；判断卡强制 `evidenceIds`；不可信 JSON 不渲染 |
| R8 | 案例本增长导致移动端存储膨胀 | 性能 | 99 条/术配额 + 归档；附件单独存；定期导出提醒 |
| R9 | 「准确率」被误读为营销承诺 | 合规 | 闭环统计仅本地个人校准；不对外宣称准确率；样本不足显式提示 |
| R10 | Node 22.17.0 与 Electron 43/44 构建兼容性 | 桌面壳构建失败 | `doctor.mjs` 前置校验；不兼容则退到 Electron 42.x 长期线实测 |
| **R11** | **引用伪造 / 幻觉引文**：模型或录入生成看似合理但不存在于原典的「引文」 | **可信度崩塌 + 可能侵权** | **① 引用必须 `segId+charRange` 可校验 ② `verify-citation` 定期跑 ③ AI 一律 E 级且标 `citationVerified` ④ 失效引用降级为「位置可能有微调」，不静默保留** |
| **R12** | **民间口诀冒充原典**：网上断语常与原句混排 | 学术诚信与用户误导 | 五档分级；D/E 强制分区；导入时来源为现代整理/网络站点 → 默认 D 级 |
| **R13** | **「过去未来」被误解为预知/回放能力** | 用户期望落空、投诉 | playbook 明示能力边界（§6.7）；系统话术「复盘与校准，不是精确回放」；禁用项拦截 |
| **R14** | **影印本 OCR 转录错误被当作原文引用** | 以讹传讹 | `transcriptionConfidence` 低值时 UI 提示「转录可能不准」；OCR 不默认启用，需授权与校对 |

**引用政策文档**：`docs/citation-policy.md` 定义分级标准、标注规范、校验流程——这是「可追溯」承诺的成文依据。

---

## 14. 工程命令与实测环境

### 14.1 统一命令入口

```bash
node scripts/run.mjs doctor                  # 环境体检（Node/JDK/SDK/Gradle/adb/磁盘）
node scripts/run.mjs install                 # npm install（workspaces）
node scripts/run.mjs build:web               # Vite 构建
node scripts/run.mjs test                    # vitest run（全量）
node scripts/run.mjs cal                     # 1900–2100 节气瞬时 + 10k 日期 diff 报告
node scripts/run.mjs golden --art=liuyao     # 生成/校验黄金样本
node scripts/run.mjs verify-board            # 与公开站快照逐格比对
node scripts/run.mjs verify-citation         # ★ 引用有效性校验（语料更新后必跑）
node scripts/run.mjs verify-playbook         # ★ playbook 完整性：每条规则有 ruleId + citations + 分级
node scripts/run.mjs android:sync            # cap sync android（主线 Capacitor 8）
node scripts/run.mjs android:build           # debug/release APK/AAB
node scripts/run.mjs desktop:dist            # Electron 打包（Win11 x64，可选 arm64）
node scripts/run.mjs kb:fill                 # 缺口回填（桌面，白名单域名）
```

### 14.2 实测环境事实（方案据此约束）

- Node v22.17.0 只能用绝对路径 `/d/Java/nodejs/node-v22.17.0-win-x64/node.exe`（PATH 里的 `/d/nodejs/node20.13.0` 是死链接）→ 写入 `.env.node`，所有脚本经 `run.mjs` 调用。
- npm 10.5.1 + npmmirror 可用；全局 `pnpm` 是坏壳 → `corepack enable pnpm` + `corepack pnpm`；已是 npm workspaces 就不强行迁移。
- JDK 17 / 21 齐备 → Android 构建固定 **JDK 21**。
- Android SDK：`android-34/36`（前瞻轨需 37）、`build-tools 34/35`、`platform-tools`、licenses 已接受。
- 主线 Gradle 8.14.3 + AGP 8.13.x；前瞻轨 Gradle 9 + AGP 9.2.x。
- **不可构建 iOS**（缺 macOS/Xcode）、**不可用 Tauri**（无 cargo/rustc）。

---

## 15. 关键文件索引

| 关注点 | 路径 |
|---|---|
| 历法内核 | `packages/core/src/astronomy/*`、`calendar/{solarTerms,ganzhi,lunar,solarTime,calibrate}.ts` |
| 插件框架 | `packages/core/src/plugins/{contract,registry}.ts`、`board/schema.ts` |
| 术数实现 | `packages/core/src/arts/{bazi,liuyao,meihua}/`（P2）；`{qimen,liuren,xiaoliuren,jinkou}/`（P4） |
| **案例本（v3）** | `packages/ledger/src/{schema,quota,store,feedback,io}.ts` |
| **事项引导（v3）** | `packages/intake/src/{taxonomy,wizard,recommend,quality}.ts` |
| **★ 指引排盘（v4）** | `packages/intake/src/playbooks/{liuyao-lost,liuyao-love,liuyao-career,past-future,...}.ts` |
| **答复装配（v3）** | `packages/answer/src/{composer,timing,safety}.ts`、`templates/*` |
| **★ 典籍查阅器（v4）** | `packages/reader/src/{citation,locate,catalog,render,annotate,backlink,collate,progress}.ts` |
| 知识库 | `packages/knowledge/src/{bm25,importer,retriever,synonyms,gap}.ts` |
| AI | `packages/ai/src/{providerRegistry,prompt,audit}.ts` |
| UI | `packages/ui/src/board/BoardRenderer.tsx`、`panels/{InterpretationDrawer,AnnotationPanel,CalibrationCard,Reader}.tsx`、`intake/IntakeWizard.tsx` |
| 平台 | `apps/desktop/src/main.ts`（Win11 + safeStorage）、`apps/android/variables.gradle`、`apps/android/app/build.gradle`、`apps/mobile/capacitor.config.ts` |
| 数据与脚本 | `data/.kb/books/`、`data/{cases,kb-gaps.jsonl,inbox,archive,notes}`、`scripts/{run,doctor,gen-golden,verify-board,verify-citation,verify-playbook}.mjs` |
| 文档 | `docs/{verification.md,compliance.md,citation-policy.md,adr/}` |

---

## 16. 技术选型联网验证报告（检索日 2026-08-29，npm registry 实测）

> 本章对计划中引用的关键外部依赖逐项联网核验（npm registry `dist-tags.latest` + 官方源站），确认版本事实、修正过期信息，并补入新发现的可复用工具。**结论先行：v5 计划的技术选型整体正确，仅 3 处微调**（TypeScript 保守线、VSOP87 校准源落地依赖、sxtwl npm 包不可用）。

### 16.1 依赖版本核验表

| 选型 | 文档声称 | npm 实测（latest，2026-08-29） | 结论 |
|---|---|---|---|
| `lunar-javascript` | 锁 minor 版本 + `LunarAdapter` | **1.7.7**（2025-11-05 发布；2026 法定假日已内置；0 依赖；近 10 个月内持续发版） | ✅ 维持；锁 `~1.7.7` |
| `@capacitor/core` | 主线 Capacitor 8，前瞻轨 9（next） | **8.5.0**（latest）；9.x 未出现在任何 latest 标签 | ✅ 双轨决策维持：主线锁 `8.5.x`；前瞻轨等官方 GA（§2.3 五项条件） |
| `electron` | 43.x–44.x | **44.0.0**（latest） | ✅ 维持；构建宿主 Node 22.17.0 满足官方要求 |
| `dexie` | 三壳统一 IndexedDB 层 | **4.4.5** | ✅ 维持 |
| `vitest` | 全量测试 | **4.1.11**（V3 线 3.2.7 仍可回退） | ✅ 采用 `^4` |
| `typescript` | — | 5.x 线 **5.9.3**；latest 标签为 **7.0.2**（原生 Go 编译器，生态兼容风险未定） | ⚠️ **微调**：采用保守线 `~5.9.3`，不追 7.x |
| `vite` | Web 壳 | **8.2.2** | ✅ 采用 |
| `astronomy-engine` | §4 研究模式 `solar-longitude-vsop87` | **2.1.19**（纯 TS、零原生依赖、`EclipticLongitude`、MIT） | ✅ **补强**：`solar-longitude-vsop87` 直接以它为落地依赖，免自研 VSOP87；同时作为简化 Meeus 的校准源 |
| `tyme4ts` | （未提及，备选） | **1.5.2**（6tail 另一套「时间」库，API 更现代） | ✅ 列为农历层备选，不替换 lunar-javascript |
| `sxtwl` | 三跑校准（CLI/Worker 独立运行） | npm 无有效 `latest` 数据 | ⚠️ **微调**：不依赖 npm 包；以 C++ 源码/CLI 独立运行，或直接用 `astronomy-engine` 作为第二校准源 |
| `iching-shifa` | （未提及） | **1.8.0**（TS：起卦、纳甲排盘、旬空、神煞；零/少依赖） | ✅ **新增对照源**：六爻机械字段（卦名/纳甲/世应/六亲/六神/旬空）交叉验证，**不复制其断语体系** |
| `mingyu-core` | （未提及） | **0.1.32**（八字/紫微/奇门/六爻/六壬/梅花/择日；依赖 `tyme4ts`+`astronomy-engine` 等） | ⚠️ **新增对照源（仅对照）**：P2–P4 黄金样本交叉验证；许可证未确认前**不作为运行时依赖**，只跑离线对比 |
| `iztro` | （未提及，★紫微新增） | **2.6.0**（紫微斗数排盘库：`astro.bySolar/byLunar`，MIT，2026-08-15 仍在发版；支持四化/亮度流派配置） | ✅ **纳入运行时依赖**：`arts/ziwei` 的安星引擎（锁 `~2.6.0` + `ZiweiAdapter`），落实 D18 |

### 16.2 关键结论与落地调整

1. **历法内核校准源升级**：v5 §4 的「研究模式 VSOP87」不再自研，改用 `astronomy-engine`（`EclipticLongitude`，纯 TS 三壳可用）；`referee` 对比层新增第三路 = `astronomy-engine` vs 自研简化 Meeus vs 历表，分歧仍进 `calibration-report.jsonl`。
2. **六爻对照实现**：`iching-shifa` 作为 fixture 黄金样本的**机械字段**交叉来源（不引入运行时依赖，脚本 `scripts/verify-board.mjs` 可拉取其 npm 版本做离线对照）。
3. **TypeScript 保守线**：全仓 `~5.9.3`，避免 7.x 原生编译器生态风险；后续评估再升级。
4. **Capacitor 维持 8.5.x**：Play target 36 合规满足；前瞻轨 9 等 GA 后按 §2.3 条件合并。
5. **Electron 44**：锁定主版本 44，43 作为回退线（R10 缓解措施不变）。

---

## 附录 A · Android 17（targetSdk 37）行为变更核对表

> 用于前瞻轨验收；每条必须有「结论 + 验证方式」。

| # | 变更 | 玄枢影响 | 状态 |
|---|---|---|---|
| 1 | 大屏强制可调（sw>600dp 忽略方向与宽高比限制） | 高 | ☐ |
| 2 | `MessageQueue` 无锁实现 | 低（禁反射私有字段的库） | ☐ |
| 3 | `static final` 不可修改 | 低（禁相关库） | ☐ |
| 4 | `ACCESS_LOCAL_NETWORK` 运行时权限 | 条件（P6 局域网功能） | ☐ |
| 5 | 标准短信 OTP 延迟 3 小时 | 无（不做短信登录） | ☐ |
| 6 | 默认启用证书透明度 CT | 低（不证书固定） | ☐ |
| 7 | 更安全的原生 DCL（`System.load` 文件需只读） | 无（不下发 .so） | ☐ |
| 8 | CP2 PII 限制与严格 SQL | 无（不读通讯录） | ☐ |
| 9 | CJKV IME 无障碍增强 | 正向（中文输入） | ☐ |
| 10 | App Bubbles / Bubble Bar / 桌面交互式 PiP | 机会（P6 浮动盘面） | ☐ |
| 11 | Activity 重建行为变更（配置变更不再默认重启） | **高（状态自恢复）** | ☐ |
| 12 | ECH 默认启用 | 调试（抓包看不到 SNI） | ☐ |
| 13 | 16KB 页大小（Play 要求，非 17 专属） | 校验 .so 对齐 | ☐ |

---

## 附录 B · 事项分类词典（初版，P1 落数据，P3 扩展）

```
求财   {正财, 偏财, 生意, 讨债, 投资, 合伙分成}
事业   {求职, 升迁, 创业, 考公, 项目成败, 职场人际}
感情   {姻缘, 婚姻, 分手, 复合, 桃花, 异地}
学业   {考试, 升学, 考证, 名次}
健康   {疾病趋势(不给诊断), 康复, 体检}
出行   {迁旅, 择日, 安全, 移居}
官非   {诉讼, 纠纷, 口舌, 违章}
失物   {财物, 证件, 宠物, 人}
择日   {婚嫁, 开业, 动土, 搬家, 签约}
家宅   {买房, 租房, 风水, 装修, 迁坟(不给确定结论)}
生育   {怀孕(不给医疗结论), 生产}
合作   {合伙, 签约, 谈判, 借贷}
决策   {A/B选择, 去留, 时机}
其他   {综合, 自定义}
```

---

## 附录 C · 答复模板示例（六爻·求财，节选）

```json
{
  "category": "求财.讨债",
  "templateId": "liuyao.wealth.debt.v1",
  "sections": [
    { "id": "conclusion", "from": "composer", "require": ["fact:财爻旺衰", "fact:世应关系"] },
    { "id": "yongshen",   "from": "core.extractFacts", "facts": ["财爻","子孙","兄弟","月建","日辰","旬空","月破"] },
    { "id": "signals",    "from": "core.rules", "rulePrefix": "liuyao.wealth" },
    { "id": "timing",     "from": "answer.timing", "rulePrefix": "liuyao.timing", "fallback": "暂无内置应期推法" },
    { "id": "counter",    "from": "core.rules + ai", "require": true },
    { "id": "evidence",   "from": "knowledge.retrieve", "minHits": 1, "fallback": "此流派暂无内置依据，请导入书库" },
    { "id": "advice",     "from": "ai.language", "safetyCheck": true },
    { "id": "disclaimer", "from": "answer.safety", "always": true }
  ],
  "forbidden": ["投资收益承诺", "具体金额保证", "法律催收建议"],
  "recordHint": "记入案例本，并标注『是否收回/收回时间/金额比例』以便后续校准"
}
```

---

## 附录 D · playbook 示例（六爻·失物，节选，对应 §6.4）

```ts
export const liuyaoLost: Playbook = {
  id: 'liuyao.lost.v1',
  category: '失物',
  version: 1,
  arts: {
    primary: 'liuyao',
    alternates: [
      { art: 'xiaoliuren', reason: '只想快速知道"现在要不要去找"' },
      { art: 'qimen',      reason: '物品可能已流出本地，需看方位与远近' },
      { art: 'meihua',     reason: '想用类象推物之形状与所处环境' },
    ],
    whyPrimary: '六爻以用神代表失物，可同时断"能否寻回 + 方位场所 + 应期"，信息量最适合寻物',
  },
  howToAsk: {
    goodExamples: ['我的身份证昨天下午在地铁站附近丢了，三天内能找回吗',
                   '家里丢了一枚金戒指，是不是被人拿走了'],
    badExamples: [{ text: '我东西丢了', why: '不指明何物，取用神会错（证件取父母爻、钱包取妻财爻）' }],
    requiredFields: ['what', 'timeRange', 'location', 'needTiming'],
    clarify: [{ id: 'isStolen', text: '你怀疑是遗失还是被人拿走？这决定要不要参看官鬼爻辨盗' }],
  },
  yongShen: [
    { condition: '现金/钱包/首饰/手机/手表/钥匙/电子产品等有价财物',
      yongShen: '妻财爻', ruleId: 'liuyao.lost.yongshen.caifu',
      citations: [/* CitationRef → 卜筮正宗·失物章 */], confidenceLevel: 'A' },
    { condition: '证件/合同/车票/书本/车/衣物/眼镜',
      yongShen: '父母爻', ruleId: 'liuyao.lost.yongshen.fumu',
      citations: [/* ... */], confidenceLevel: 'A' },
    { condition: '宠物/玩具/活物/药品',
      yongShen: '子孙爻', ruleId: 'liuyao.lost.yongshen.zisun',
      citations: [/* ... */], confidenceLevel: 'A' },
    { condition: '疑似被盗，需辨盗',
      yongShen: '官鬼爻', ruleId: 'liuyao.lost.yongshen.guigui',
      citations: [/* ... */], confidenceLevel: 'A' },
  ],
  locating: {
    byYaoWei: { 1: '地面墙角/鞋柜/地下室', 2: '厨房/卫生间/床底', /* ... */ },
    byDiZhi: { 子: { dir: '正北', places: ['水边','冰箱','卫生间','潮湿角落'] }, /* ... */ },
    byLiuShen: { 青龙: '干净显眼处', 勾陈: '堆叠杂物下被压', 玄武: '隐蔽暗处抽屉深处', /* ... */ },
    ruleId: 'liuyao.lost.loc',
    citations: [/* ... */],
    confidenceLevel: 'D',   // ★ 此类细化定位多来自民间传承整理，无原典逐字对应者标 D
  },
  timing: {
    rules: [{ name: '静待冲、动待合、空待出空、墓待冲墓、破待补破、伏待冲飞',
              ruleId: 'liuyao.lost.timing.general', citations: [/* ... */], confidenceLevel: 'A' }],
    fallback: '此卦象暂无可用的内置应期推法，建议记录并事后回标实际时间以校准',
  },
  readingList: [
    { canonicalId: 'bushizhengzong', book: '卜筮正宗', chapter: '失物章',
      why: '取用神"各归其类，物有所主"的原始论述', priority: 1 },
    { canonicalId: 'zengshanbuyi', book: '增删卜易', chapter: '失物章',
      why: '用神宜旺不宜空破；鬼动克世之辨', priority: 1 },
    { canonicalId: 'huangjince', book: '黄金策', chapter: '失物',
      why: '"物失难寻，凭用神之存亡"', priority: 2 },
  ],
  forbidden: ['不得指名道姓断言某人盗窃', '不得教唆搜查他人身体或住宅',
              '贵重物品被盗应提示报警并保留证据，卦象不作为法律证据'],
  disclaimer: '本答复不构成对物品下落或他人行为的确定性判断，请理性处理并依法维权',
  recordTemplate: {
    fields: [
      { key: 'object',   label: '丢失何物', type: 'text' },
      { key: 'lostAt',   label: '丢失时间地点', type: 'text' },
      { key: 'found',    label: '是否找回', type: 'enum', options: ['是','否','部分'] },
      { key: 'foundAt',  label: '实际找到位置（对照卦象方位）', type: 'text' },
      { key: 'foundTime',label: '实际找回时间（对照应期）', type: 'date' },
    ],
    hint: '记录实际找到的位置与时间，与卦象方位、应期对照，这是提升断准率最快的方式',
  },
};
```

---

## 附录 E · CitationRef 示例

```json
{
  "canonicalId": "zengshanbuyi.ws.1912",
  "book": "增删卜易",
  "author": "野鹤老人",
  "edition": "民国三年校经山房石印本（公有领域转录）",
  "chapter": "卷三·失物章",
  "segId": "zsby.3.12",
  "charRange": [18, 62],
  "quote": "用神宜旺，不宜空破。用神若得月建日辰生扶，纵暂时不见，终能找回",
  "page": "卷三 叶十二",
  "license": "公有领域",
  "sourceUrl": "https://zh.wikisource.org/...",
  "accessedAt": "2026-08-29",
  "confidenceLevel": "A",
  "transcriptionConfidence": 0.96
}
```

---

## 附录 F · 引用分级速查

| 级 | 名称 | 内容 | 角标 | 可否为主依据 |
|---|---|---|---|---|
| A | 原典原本 | 公有领域原典原文 | 绿「原典」 | ✅ |
| B | 历代注疏 | 古人注、疏、集解 | 蓝「注疏」 | ✅ 需标注家 |
| C | 现代整理 | 点校本、白话译 | 灰「现代整理」 | ⚠️ 仅辅助理解 |
| D | 流派说法 | 口诀、师承、网络整理 | 橙「流派说法」 | ⚠️ 需注明非原典 |
| E | AI 生成 | 模型输出 | 红「AI 生成」 | ❌ 不可 |

---

## 17. 实现现状与验证记录（2026-08-29 实测，工程已落地）

> 本章为工程实际落地状态（与 v5 计划的对照），凡「已实现」均经过 `vitest` 全量测试与脚本校验，数据可信。

### 17.1 已落地清单

| 模块 | 状态 | 说明 |
|---|---|---|
| Monorepo（npm workspaces） | ✅ | `packages/{core,ledger,intake,answer,reader,knowledge}` + `apps/web`；`.npmrc`/`.env.node`/`run.mjs`/`typecheck.mjs` |
| 历法内核 | ✅ | JDE/ΔT、太阳视黄经（简化 Meeus + **VSOP87(astronomy-engine)** 双实现）、均时差、真太阳时、节气定气（逆迭代求交）、干支/纳音/藏干、月柱（节气换月）、农历适配层（lunar-javascript） |
| 六爻引擎 | ✅ | 六类起卦、京房八宫生成、纳甲、六亲、世应、六神、旬空月破日冲、动变互、伏神、断卦规则（ruleId + 周易原文引用） |
| 八字引擎 | ✅ | 四柱（立春/正月初一换年、节气换月、23 时切日）、十神、纳音、大运起运（顺逆 + 3 天 1 岁）、神煞 9 类 |
| 梅花引擎 | ✅ | 时间/报数/字占、体用生克、互变卦 |
| 紫微引擎 | ✅ | `iztro` 适配层（D18/D19）：十二宫、主星、五行局、命主身主、大限、configHash 可复现；**工程实现细则见 v8 §7.8**（数据模型/时辰映射/适配层接口/iztro 输出规范化事实/测试矩阵） |
| 案例本 ledger | ✅ | 99 条配额/软提醒、重复判定、闭环统计、JSON/CSV/Markdown 导出；**`LocalCaseStore` 持久化（localStorage 注入式后端，刷新/重启不丢，损坏数据安全降级）+ Web 端导出/导入按钮** |
| 事项引导 intake | ✅ | 14 类词典、**十二张 playbook**（失物/感情/事业/求财/学业/出行/官非/合作/决策/健康/紫微事业/过去未来，九段路径）、6 步向导、问句质量提示、敏感拦截 |
| 答复装配 answer | ✅ | 四层装配（模板+事实+规则+证据）、安全拦截、反推拦截（D26）、免责注入 |
| 知识检索 knowledge | ✅ | CJK unigram+bigram BM25、同义词扩展、事项联动；**内置公有领域语料 13017 段（《周易》448 + 《增删卜易》3565 + 《卜筮正宗》2076 + 《黄金策》3579 + 《易冒》3349）启动即预导入**，断语引用自动回链原文并补 charRange（`enrichRuleCitations`）；`scripts/fetch-diancang.mjs` 拉取源文本 → `scripts/kb-build.mjs` 切分（支持独立标题行 / 内嵌章节标题两种模式）生成 `corpus.jsonl` 与内置 TS 模块 |
| 典籍阅读 reader | ✅ | CitationRef 校验、L1 charRange 定位、Text Fragment/IIIF 预留、书架目录树 |
| **AI 精解 ai（★新增）** | ✅ | ProviderRegistry（7 家厂商 + 自定义）、提示工程（禁止算盘 + 引用纪律）、OpenAI 兼容客户端（JSON 契约 + 正则修复 + 降级）、调用审计；Web 端「AI 配置 + AI 精解」面板已上线（Key 仅存内存） |
| Web 工作台 | ✅ | Vite 8 + React：**四术切换（六爻/梅花/八字/紫微）** + 通用盘面渲染 + 内置知识库 + AI 配置与精解 + 存档，全流程可用 |
| **Android（Capacitor 8）** | ✅ | `apps/web/android` 工程已建：minSdk 24 / target 36 / AGP 8.13 / Gradle 8.14.3；`android:build` 出 debug APK → `release/xuanshu-debug.apk`（约 5.14 MB，包名 `com.xuanshu.workbench`） |

### 17.2 验证数据（全部实测）

- `npm test`：**112 个测试全部通过**（12 个测试文件）；`npm run typecheck` 6 包全绿。
- **节气精度**：2020–2026 全部 168 个节气与 lunar-javascript 历表交叉验证，**最大误差 0.7 分钟**（v5 阈值 2 分钟）。
- 日柱锚点与 lunar-javascript 连续 **500 天交叉一致**。
- `verify-playbook`：4 张 playbook 全部合规（每条 ruleId + citations + confidenceLevel）。
- `golden`：六爻 3 例 + 八字 3 例黄金样本全部通过。
- Web 构建：初始 JS gzip ≈ 20KB（astronomy/iztro 懒加载分包），符合 §7.4 预算。
- **Android 实测（LDPlayer 14 模拟器）**：`adb install` 成功；应用在前台无崩溃；CDP 验证 React 渲染、**完整走通「起卦→排盘（山泽损）→断语（ruleId+出处）→存档案例本」**；日志仅一条 Capacitor safe-area 无害告警。
- **Android 复验（多术数 + AI + 内置知识库）**：四术 Tab 全部可用——六爻（断语+原文回链）、梅花（雷天大壮·体用生克）、八字（庚辰年甲申月乙巳日戊寅时·大运神煞）、紫微（木三局·十二宫·命宫主星）；内置《周易》448 段预导入；AI 精解面板（7 家厂商）就绪。
- **Android 复验（知识库扩展《增删卜易》）**：内置语料扩至 **3995 段**（《周易》448 + 《增删卜易》240 章 3547，句级切分、章节层级保留）；六爻断语原文内联由 3 处提升至 **7 处**——旬空/月破/六冲/六合/暗动/应期规则全部回链《增删卜易》逐字原文（如「静而逢值逢冲」「月建冲之为月破」）；`scripts/kb-build.mjs` 可重复生成 `corpus.jsonl` + TS 模块。
- **Android 复验（知识库扩展《卜筮正宗》）**：内置语料扩至 **4918 段**（+《卜筮正宗》十四卷 923 段，含「用神分类定例/月破论/旬空论/黄金策千金赋直解」等章节）；来源 diancang.xyz 公有领域转录（GBK/双编码问题已规避，UTF-8 干净文本）；全量测试 **133/133 通过**。
- **工程坑位记录**：① 本机 `jdk21.0.7` 是 **Oracle GraalVM 21**（jlink 与 AGP 不兼容）→ 已装 Temurin 21（`D:\Java\jdk\jdk-21.0.12.1+1`）；② Gradle 发行包与 Maven 依赖在部分网络下需走镜像（wrapper → 腾讯 `mirrors.cloud.tencent.com/gradle`，repos → 阿里云 `maven.aliyun.com`）；③ **环境陈旧 `JAVA_HOME=jdk1.8` 会致 AGP 构建失败**，`run.mjs android:build` 已强制锁定 Temurin 21。
- **完善（playbook 扩充 + 案例本持久化）**：① 事项卡由 4 张扩至 **12 张**——新增求财/学业/出行/官非/合作/决策六爻卡、健康（八字主用）卡、紫微事业卡，全部引用《黄金策》《增删卜易》**逐字原文（A 级）**，验证 `verify-playbook` 12 张全部合规；② `LocalCaseStore`（ledger）实现 localStorage 持久化案例本，Web 端刷新/重启不丢，新增 JSON/CSV/Markdown 导出按钮与案例导入合并；③ Android 模拟器 CDP 全功能验证 **18 项全部通过**（含事项卡联动、导出按钮、存档持久化、四术排盘）；全量测试 **139/139**、typecheck 7 包全绿。
- **完善（知识库 IndexedDB 落库）**：① `Bm25Index`/`Retriever` 新增**可序列化快照**（docs/postings/docLen/avgLen + 语料 sections）；② 新增 `persist.ts`：`KnowledgeStore` 接口 + **原生 IndexedDB 实现**（库 `xuanshu-knowledge`，零新增依赖，隐私模式降级内存）+ 内存实现；`corpusHash`（FNV-1a）随语料变化，**版本不符或快照损坏自动重建**；③ Web 端启动**同步兜底 + 异步恢复**：二次启动命中 IndexedDB 快照免重建 BM25 索引（UI 显示「IndexedDB 快照」标记）；④ 全量测试 **145/145**；Android 模拟器验证：首次启动落库成功（13017 段 / 5 书 / hash `fnv1a_b2f97496`），重启命中缓存，全功能 18 项 + 知识库专项全部通过。

### 17.3 尚未落地（对应 P2 后续 / P3–P6）

- Electron（Win11）与 Capacitor（Android）壳、safeStorage/EncryptedSharedPreferences 密钥链路；
- 奇门/大六壬/小六壬/金口诀四术（P4）；
- 知识库用户自有书库导入的 IndexedDB 多版本管理与增量索引（当前落库仅覆盖内置语料，导入管线留待 P3 阅读器完整版）。

---

**文档结束（v5 合并完整版）。**

本计划合并 v3（平台适配 / 99 条案例本 / 事项引导与精准答复）+ v4（指引排盘 playbook / 典籍查阅器与批注溯源），并补齐交叉缺漏（历法内核、五术范围与黄金样本、AI Provider 表、工程命令与实测环境、引用校验与分级政策、附录 A–F）。

**七大补充（D1–D17）全部锁定**：Android 16/17 双轨、Windows 11、99 条/术案例本、精准答复、指引排盘、典籍查阅器溯源、历法内核可复算。凡引用外部事实（Android 版本、Capacitor 矩阵、Play 政策、Electron 平台支持）均已标注来源与检索日期；无法验证的项（iOS 构建、奇门少数派、六壬来意占、真实 AI 接口、IIIF 影印本）一律直说，不包装成确定结论。

**两项已识别、留作后续决策的开放点**（不影响本计划落地）：① Play 的 target 36 死线（2026-08-31）已过，P5 出包大概率晚于该日，需走延期申请或先以 Web/桌面分发；② Capacitor 9 当前仍在 `next` 文档通道，前瞻轨合并主线的五项条件以官方 GA 为准。
